{
  "name": "ties-break",
  "private": true,
  "version": "0.1.0",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "check": "vue-tsc -b --force && vitest run --project unit && vite build",
    "icons": "node scripts/gen-icons.mjs",
    "art": "node scripts/optimize-art.mjs",
    "bench:econ": "vite-node tools/econ-bench.ts",
    "bench:fatigue": "vite-node tools/fatigue-bench.ts",
    "bench:knock": "vite-node tools/knock-rate.ts",
    "bench:load": "vite-node tools/load-bench.ts",
    "bench:radar": "vite-node tools/radar-bench.ts",
    "test": "vitest run --project unit",
    "test:watch": "vitest",
    "test:sim": "vitest run --project sim",
    "test:all": "vitest run"
  },
  "dependencies": {
    "pinia": "^3.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "fake-indexeddb": "^6.0.0",
    "sharp": "^0.35.3",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vite-plugin-pwa": "^1.0.0",
    "vitest": "^3.2.0",
    "vue-tsc": "^3.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
