---
type: spec
status: draft
area: engine/balance
canonical: false
last-reviewed: 2026-08-17
---

# The 250 is not a 1000 – the head of the entry list, and the Slam door at 112 (17.08.2026)

Round 21, items 4 and 2. **Item 4 ships an engine change**; item 2 ships a one-number change and a
wild-card proposal that is **costed and NOT built**, which is the owner's own instruction.

The evidence this stands on is `docs/specs/round21-measured-2026-08.md` §3f and §5c. Nothing measured
there is re-litigated here: her build is **above** her band (core 57.1 against 50.4), the match model
gives P(QF+) = 15.0% at a 250, and her two-season drought is a 20.5% event. **The defect is the FIELD.**

---

## 1. THE PREDICTIONS, WRITTEN BEFORE THE RUN

Committed in `1ffe7c1` before a single measurement, so the column below is a prediction and not a
description. **The storey arithmetic they come from**, since a prediction with no derivation is a
guess with a decimal point: `FIELD.tiers` makes the merged 1,799-row table `#1-64` *tourElite* (core
67-77, mean 72), `#65-94` *elite* (56-66, mean 61), `#95-214` *contender* (43-53, mean 48). And
`selectEntrants` keys entry as `position + rng x drawSize`, so a 32-draw consumes about **fifty
consecutive positions** from the head of its band – which is why the shipped 250's window `#32-#82`
predicts a field core of `(33x72 + 18x61) / 51 = 68.1` against the **68.4 measured**. The model works,
so it is used to predict the sweep.

| `wta250.acceptsFromRank` | window | predicted field core | predicted P(QF+) | predicted exp pts |
| --- | --- | --- | --- | --- |
| OFF (shipped) | #32-#82 | **68.4** – must reproduce §3b exactly | **15.0%** | **20.1** |
| #40 | #40-#90 | 66.4 | ~19% | ~26 |
| **#50** | #50-#100 | **62.7** | ~28% | ~40 |
| #64 | #65-#115 | 55.6 | ~48% | ~68 |
| #80 | #80-#130 | 51.8 | ~60% | ~85 |

**PREDICTED PICK: #50** – and the criterion is stated in advance rather than fitted afterwards:

1. the WTA 250 must stop being **the worst rung in the game** – it must pay more than a W50's **29.7**;
2. it must pay more than the WTA 125's **35.5**, because it is the rung above it;
3. it must NOT pass the W100's **57.7**, or the fix has simply moved the farm one storey up and the
   owner gets a new version of the same complaint;
4. no other rung's field may move at all.

**Predicted collateral**, all of which is reported below whether or not it came out flattering:

* **P6** – field core and expected points at every other rung: **unchanged**, to the noise of 300 replays.
* **P7** – her seed index at a 250: **still unseeded (32)**. The field weakens; she is still #121 and
  still the bottom of it. If the fix worked only by seeding her it would be the wrong fix.
* **P8** – `ladder-baseline --seeds 10` **will move, and it should**: §3 of the baseline says the WTA
  250 is the most-entered professional rung in the game from age 19 (**5.0-5.5 entries a year**), so
  making it winnable is a change to the whole ladder. Predicted career-high median **#112 → #95-#105**.
* **P9** – the frozen MAIN capture (41550 / `e6b0c709`): **untouched**. `selectEntrants` runs on
  `seed:aitour:` / `seed:kidtour:` sub-streams only.
* **P10** – the frozen careers in `tests/coach-travel-edge.test.ts`: **will move**, per-key diff first.

---

## 2. WHAT CHANGED

| file | change |
| --- | --- |
| `src/engine/season/types.ts` | **`TierDef.acceptsFromRank`** – the HEAD of a rung's entry list, the other end of `acceptsRank` |
| `src/engine/season/calendar.ts` | `wta250.acceptsFromRank = 50`; `slam.acceptsRank` **104 → 112** (§6) |
| `src/engine/season/tournament.ts` | `selectEntrants` reads the head, and yields to fillability |
| `tools/big-rung-odds.ts` | **extended** – `--head-sweep`, so the number is chosen by measurement |

**One sentence:** `acceptsRank` says where a rung's entry list stops; `acceptsFromRank` says where it
starts. A player ranked inside the head is not refused a WTA 250 – **she is at a 1000 or a major that
week**, which is what the real commitment system does and what
`docs/research/ranking-points-by-tier.md` §4c-C means by *"Reality gates the strong OUT; we gate the
weak IN."*

⚠ **It is read by `selectEntrants` only – the AI FIELD.** The symmetric half that would refuse HER a
WTA 250 once she is inside the head is a second decision and is **deliberately not taken** – §5.
