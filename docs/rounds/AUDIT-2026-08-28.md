---
type: round-audit
status: current
area: rounds/audit-2026-08-28
canonical: false
last-reviewed: 2026-08-28
---

# Audit of rounds 3–28 – what is still forgotten (28.08.2026)

> ⚠⚠ **LANDED 01.09.2026, AND IT IS A SNAPSHOT OF 28.08 – NOT A CURRENT LIST.** This document sat on
> an unmerged branch for four days while rounds 29, 30, 31 and 32 shipped, so items it calls open may
> since have closed. It is landed as the dated record it is, because an audit nobody can find is the
> defect it was written about. ⭐ Its headline finding WAS re-checked against `main` on 01.09 and is
> **still live** – see `docs/rounds/round-33.md` item 2, which carries the re-measurement.

Round 29 item 17, his own ask: «проверь предыдущие раунды на предмет "что забыто и не сделано"
пожалуйста». Read-and-verify only – **nothing below was fixed, and `src/` was not touched.** The
frozen MAIN capture is UNMOVED: `tests/condition.test.ts:138` still reads `count: 41550`, hash
`e6b0c709`.

This is the SECOND audit of this folder. The first, [AUDIT-2026-08.md](AUDIT-2026-08.md) (13.08),
covered rounds 8–17 and found 14 open, 9 dropped, 3 regressed. **This one re-checks its findings
against today's tree and covers the rounds it could not: 3–7 and 18–28.**

## The verdict, in six sentences

**Fifteen days later, not one of the 13.08 audit's top five has moved.** The regression it put first
– the Stats header tiles – is still on his screen, still clips, and still has no test; three of the
items it called "still open" are still open with zero code, and two of them are the same item asked
four times. **The newer rounds are in far better shape than the old ones**: 19–25 and 27 are honest,
every `[~]` in round 23 carries a real answer under it, and a ten-mark `[x]` sample came back with
one false and one half-false, both of them already known. **The rot has moved from the ledgers to the
delivery page**: `docs/now-next-later.md` (reviewed 27.08) carries three of roughly twenty open
items, so the rest are recorded but nobody is holding them. **The index table has rotted a fifth
time** – `README.md` has no row for rounds 26, 27 or 28. And **round 26 #1 is round 29 #6**: the
span pill was re-gated on his rule but was never introduced, so he pressed it again this week and
still does not know what it is for.

| round | still open | silently dropped | regressed |
| --- | --- | --- | --- |
| 3 (22.07) | 5 – all correctly reasoned | 0 | 0 |
| 4, 6 | – | 0 | 0 |
| 5 (23.07) | 4 – all confirmed absent from `src/` | 0 | 0 |
| 7 (24.07) | 1 – **and its stated blocker dissolved** | 0 | 0 |
| 8 (25.07) | 1 – the oldest, 34 days | 1 (R8-10's second half) | 0 |
| 9 (25.07) | 1 – **and it blocks round 18 #11** | 0 | 0 |
| 10 (26.07) | 0 | 1 (R10-1) | **1 – LIVE, unpinned** |
| 11 (27.07) | 1 | 0 | 0 |
| 12, 13 | 0 | 1 (economy decision 2, since moving) | 0 |
| 14 (06.08) | 1 – needs a ruling | 1 (the art question) | 0 |
| 15 (09.08) | 4 – three untouched since 02.08 | 1 (**kit wear – the worst**) | 0 |
| 16 (11.08) | 3 | 2 | 0 |
| 17 (12.08) | 3 – two `[>]` for 16 days | 2 (both questions) | 0 |
| 18–25 | 0 open that is not parked on him | 0 | 0 |
| 26 (24.08) | 1 (#1's sentence) + #14b on him | 0 | 0 – but 2 stale `[!]` boxes |
| 27 (27.08) | 5 – four parked on him, **#8 is not** | 0 | 0 |
| 28 (28.08) | 7 – round 29 re-files three of them | 0 | 0 |

---

## By round, oldest first

Only rounds with a finding are listed. **Rounds 4, 6, 9 (bar R9-12), 12, 13, 18, 19, 20, 21, 23, 24
and 25 are clean** – their boxes match the tree, their answers exist, and what is open in them is
parked on him with a current reason.

### Round 3 – clean as a ledger, five genuinely open

Team card, childhood prologue (backlog #71), Moments gallery, the weather's mechanical half, and
mom/dad at onboarding. All five verified absent today (`git grep` for `TeamCard`, `Moments`,
`parentRole`, indoor/outdoor mechanics: nothing but comments saying so, and `KidScreen.vue:523` says
Moments «does not exist» in its own words). Every reason on the file is current. **Nothing to revive
– this is backlog, correctly kept.**

### Round 5 – clean, four open

Player-uuid exchange, relationship/trust UI, attend-vs-watch-on-TV, and the wealthy-track academy
invitation: all four confirmed to have no code at all. Correctly recorded.

### Round 7 – 1 STILL OPEN, and its blocker is gone

«Экраны детализации по дням» was deferred with a condition: *"не будет иметь смысла, пока не появится
система 'тренировка даёт эффект'"*, and re-read on 09.08 as "no longer blocked, design in progress on
`docs/training-dials`". **Those controls shipped** – `src/components/HerWeekTab.vue` is the per-day
grid (training-dials §9) and `src/composables/weekDays.ts` is its model. Nobody has re-read the box
since. → *What it would take: one sentence from him on whether he still wants a per-day VIEW now that
the per-day CONTROLS exist.*

### Round 8 – 1 still open (34 days), 1 still dropped

**R8-1, the in-tournament player card** – still untouched, now the oldest open item in the folder by
a month. Tracked on `now-next-later.md`. **R8-10's second half** (the post-match review popup and the
"after which events, how thoroughly" knobs) is still dropped; it is now tracked from `training-dials`
§7/§8, which are themselves still unbuilt. → *One screen; one design question.*

### Round 9 – R9-12 morale, and it is a blocker on a second round

`morale` has six occurrences in `src/` and **every one is a comment saying nothing touches it**
(`world/birthday.ts:29`, `:1275`, `world/state.ts:116`, `protocol/narrative.ts:103`,
`MatchViewer.vue:846`). Round 18 #11 (federation penalties for anger) was parked "in the morale wave
(#95)" on 13.08 and the wave has never been scheduled. **Two rounds, one stat that does not exist, 34
days.** → *Not a build decision now – a queue decision, or an explicit "never".*

### Round 10 – ⚠ THE ONE LIVE REGRESSION, and it is fifteen days old as a filed finding

`[!] R10-2 / R10-8, the Stats header tiles.` Confirmed still broken today:

- `StatsScreen.vue:243` renders `{{ LADDER_LABEL[shown] }} rank` and `:254` renders
  `{{ LADDER_LABEL[shown] }} W–L`.
- `LADDER_LABEL` (`shared/protocol/ladder.ts:118`) is `National / International / Professional`, so
  the longest labels on that row are **"International rank" and "International W–L", 18 characters**,
  against the **11-character "Junior rank"** this item was measured at on a 375pt screen.
- `.stats-tile-label` (`style.css:2718`) is `white-space: nowrap`, which **clips rather than wraps**.
- The screen's own comment above the row still claims the fix – *«"Season points" is now "Season
  pts", which is what actually fits at 375px»* – and **that string does not exist anywhere in `src/`.**
- **No test pins any of the three labels**: `git grep "stats-tile-label" -- tests` returns nothing.

→ *What it would take: one decision on the row – shorten the label, drop the ladder word from two of
three tiles, or let it wrap – plus the mounted assertion that has never existed.* **He reads Stats
every season.**

R10-1 (world news and events) is still one line of itself, as recorded.

### Round 11 – R11-1b, and it is cheaper than it was

`world/injury.ts:224` writes `injuryHistory` and caps it at 20 entries; `injuryTau` reads **none of
it**. The data has existed since July and nothing consumes it. → *One term in an existing function.*

### Round 14 – #17 open, the art question still never asked

The difficulty wrapper still needs a ruling, not a build. The group-F art question – *"if there is a
further art ask, it needs his own sentence"* – has still never been put to him.

### Round 15 – 4 open, and the dropped one is the worst item in this audit

- **#14 / ruling 5, a vacation pauses kit wear.** `src/engine/equipment.ts` contains **no vacation
  term anywhere in the file**; `kitAgeWeeks` is pure elapsed calendar weeks off two clocks. Ruled by
  him 09.08, re-asked by him as round-16 #8 on 11.08, named #4 on the 13.08 audit's own shortlist,
  and **19 days later there is still not one line.** Four askings, one ruling, no code – while
  `injuryTau` has had its vacation factor since round 12. → *One factor, one bench line.*
- **#6, the W100 / World Tour 125 cadence.** `calendar.ts:867` and `:964` still read
  `everyNWeeks: 13` – four events a season each, and `national`/`j300` are 13 too. Last edited
  02.08; nobody has looked at it.
- **#3, the remaining-events counter on the W cards.** No per-card counter exists; the only supply
  figure is still the Season header line.
- **#9, the J300-vs-W15 field asymmetry** – no probe in `tools/`, still.
- **#13, "Training week" over a tournament week** – unfixed, never reproduced, and the published
  diagnosis is still the wrong one.

### Round 16 – 3 open, 2 dropped

- **#20, keep the screen awake.** `wakeLock` still returns **zero hits in the entire repo** outside
  the ask itself. → *A dozen lines behind a `navigator.wakeLock` guard.* Cheapest item here.
- **#8** is round 15 #14 above. **#6** is still not reproduced.
- **#10, `key` as a highlights reel** – still dropped, no round has picked it up. Smaller than it
  reads: the short match already exists.
- **#11's register ladder** shipped with two storeys where his ruling describes three; the shortfall
  is still only in the spec and has still never been said out loud.

### Round 17 – 3 open, 2 dropped, and one flagged surface never fixed

- **#15 and #22 are still `[>]` "awaiting his word" – 16 days.** #22 (rivals in the commentary) was
  priced at ~20 lines with no schema change and there is still no rival concept in `src/viz` or the
  commentary.
- **#17's NEED half** was never put to him, so a rich family with no sponsor still shops the
  classifieds.
- **#H**, domestic rungs in a 22-year-old's list, task #84 – open.
- ⚠ **#28's flagged surface is still unfixed.** `TierGuide.vue:49` still builds `entryFee` as
  `formatCents(t.entryFeeCents)`, so the guide prints **`$0`** where every other call site says «no
  entry fee». Flagged 13.08. One line.

### Rounds 19–25 – clean

Everything open in them is parked on him with a live reason and is on `now-next-later.md`'s *Later*
list: round 22's one real-world term (`calendar.ts:1341`, `label: 'Grand Slam'`) and round 25 #9 (the
injury rebalance for careless play, waiting on him alone since 24.08). **Round 23 deserves saying out
loud**: every one of its six `[~]` items carries a dated answer under the brief, which is exactly the
discipline round 27 #13 failed.

### Round 26 – #1 is open, #14b is on him, and two boxes are stale

- **#1 is genuinely open and it is round 29 #6.** The second pass shipped both halves of his 25.08
  ruling – position (`weekAction.ts:50`, first child of `.next-week-bar`) and condition
  (`spanWorthOffering`, 204/208 weeks → 5/208). **But the ORIGINAL item was «answer + possibly build
  (a first-use line)» and no such line exists.** He asked what the button was; it was re-gated and
  never introduced. This week he pressed it again and reported that it is «весьма бессмысленной».
  → *Fixing round 29 #6's three bugs without a sentence that says what the control is for ships this
  complaint a third time.*
- **#14b `[?]` – a measured overshoot, correctly parked.** Pro-era injury prevalence reads **71%
  against the 30–54% professional band**; the wave refused to move it under his live K=8/F=0.3 dose
  test, which was the right call. **That test is over.** ⭐ It is the same thread as round 25 #9 and
  round 28 #5 – see the shortlist.
- **Two stale `[!]` boxes over shipped work**, the round-15/21 failure again: "2 (again)" appears at
  `:438` as `[!]` and at `:439` as `[x]`, and "10 (again)" at `:523` as `[x]` and `:572` as `[!]`.
  The work is in the tree – `announceCampusInterlude` / `campusDigestLine` live in
  `world/fieldNews.ts` and are called at `world.ts:1883`. **Doc tidy, not work.**

### Round 27 – four parked on him, one that is not

1 / 1a / 1b (one line, waiting on the entry-age band), 3 (waiting on the growth curve) and 14 (held
on purpose so it is not fixed in the same breath as findings pointing the other way) are all
correctly parked, and the file's own "waiting on" table says so.

⚠ **#8 is the exception and it has no owner.** «в History расход за сезон написан 36 тысяч, а на
вкладке расходов 25 тысяч» was **reproduced exactly on his own save** and diagnosed: the arithmetic
is right, and **two screens say «за сезон» about different seasons** – the tab shows season 9 at 34
of 52 weeks, History shows season 8 complete. The ledger calls it *"a label, not a repair"*, and then
nothing moved. → *One label on each of two screens.* He is still walking around believing «явно
что-то не ладно с нашей математикой» when nothing is.

### Round 28 – open, and round 29 already re-files three of them

3, 5, 7, 9, 12, 14, 18 stand open or on him. Round 29 re-files #7 as its #5 (the shop), #9 as its #12
(his ruling on current-account interest) and #1/#6 as its #3 (the shoot on a tournament week). **#18,
the save-scumming question, is filed correctly** and needs a spec before anything is built – it
touches the one law nothing else may touch.

---

## The `[x]` sample – ten checked, one false, one half-false

⚠ These marks were written by the agents that did the work and had never been independently checked.
Sampled for what a player would notice.

| claim | verdict |
| --- | --- |
| R10-2 / R10-8 Stats tiles | **FALSE** – still regressed, see round 10 above |
| R17 #28 «no entry fee» | **HALF-FALSE** – `TierGuide.vue:49` still prints `$0` |
| R20 #3 a dialog fits a phone | **VERIFIED at the shell**, which is stronger than per-dialog: `.dialog-card` now carries `max-height: 100%; overflow-y: auto` (`style.css:1644`), so all twelve dialogs are bounded. `tour-briefing.test.ts` 14/14 green |
| R21 #10 the kit countdown | **VERIFIED** – `MoneyScreen.vue:1242`, `({{ view.goodWeeksLeft }} left)` |
| R22 fictional tier labels | **VERIFIED** – all fifteen rungs are `Local Open` / `Junior Tour n` / `World Tour n`; the single real term `Grand Slam` (`calendar.ts:1341`) is flagged and parked on him |
| R23 #2 the suspension closing line | **VERIFIED against a real mounted render** – `round23-tour-suspension.test.ts` 2/2 green, and reverting the copy reddens it |
| R26 #10-again the campus digest | **VERIFIED** – `world/fieldNews.ts`, called at `world.ts:1883` |
| R26 #1-again the pill's position and gate | **VERIFIED** – `weekAction.ts:50` + `spanWorthOffering` |
| R27 #4 the college ladder label | **VERIFIED**, pinned twice (`round26-college-flow.test.ts:425`, `round27-call-up-flow.test.ts:415`) |
| R28 #1 masseur days on the calendar | **VERIFIED** – `weekDays.ts:227-235` |

**And the invariant**: `tests/condition.test.ts:138` reads `count: 41550`, hash `e6b0c709`. Unmoved.

## What could not be reconstructed

The 13.08 audit flagged a gap: the 29.07–01.08 build waves used a **second, unrelated round
numbering** and never got ledger files. Re-checked today:

- **`fde515d` (01.08, "round 15: nine owner items") CAN be reconstructed** – its commit body carries
  all nine items with his quotes (goal ladder, WTA chip, whole-painting scene, kit stand-down, first
  cheque, W reprice, cover 0.75, summer weeks, sliding window) and each is described as shipped with
  its file. **It does not need his sentence; it needs a ledger file if anyone wants one.**
- **`fix/round-19` / `fix/round-20` / `fix/round-21` (31.07) CANNOT.** They are merges of feature
  branches – `feat/trophy-cabinet`, `fix/match-screen`, `feat/offers-inbox-slice` and so on – with no
  owner numbering and no quoted ask anywhere in the subjects. If items were lost there, **only his
  own list can recover them**, and I am not going to infer one from branch names.

## ⭐ What is worth reviving now, ranked – a player-visible defect outranks a doc tidy

Eight, and deliberately not more. Everything above this line is recorded; **this is the part that
should be scheduled.**

1. **The Stats header tiles clip (round 10, `[!]`).** The only confirmed live regression in the
   folder, on a screen he opens every season, and no test pins it. One decision on the row plus the
   mounted assertion that has never existed. *It was #1 on the 13.08 list too.*
2. **Kit wear runs on holiday (round 15 #14 / ruling 5 / round 16 #8).** Ruled once, asked four
   times, zero lines in nineteen days. One term in `kitAgeWeeks` and a bench line – `injuryTau` has
   had its vacation factor since round 12, so the shape already exists.
3. **The screen still sleeps during a match (round 16 #20).** Zero code in the repo. The cheapest
   item on this list and the most physically annoying while he plays.
4. **Round 26 #1's missing sentence – and it is this round's #6.** The span pill was re-gated and
   never introduced. Whoever takes #6 should ship the first-use line with the bug fix, or the same
   complaint arrives a third time.
5. **Round 27 #8, the two «за сезон» numbers.** Reproduced on his own save, diagnosed as a label and
   not a repair, then nobody took it. Two labels. He is currently walking around believing «явно
   что-то не ладно с нашей математикой» when nothing is – which is worse than the bug would be.
6. **The injury thread, as ONE ruling and not three items** – round 25 #9 (`[?]` on him since
   24.08), round 26 #14b (`[?]`, measured at **71% prevalence against a 30–54% band**) and round 28
   #5. #14b was correctly refused mid-way through his live K=8/F=0.3 dose test; **that test is
   over**, so the refusal has expired and he owes one decision, not three.
7. **Morale (round 9 R9-12), and round 18 #11 behind it.** Thirty-four days, two rounds, one stat
   that does not exist and has never been scheduled. Not a build decision – a queue decision, or an
   explicit «never» so round 18 #11 can be closed instead of parked.
8. **`TierGuide.vue:49` prints `$0`** where every other surface says «no entry fee». Flagged 13.08,
   one line, still there.

**Below the line, doc tidy only** – worth doing in the same pass as anything else that touches these
files, never on its own: the `README.md` rows for rounds 26–28; round 26's two duplicated `[!]`/`[x]`
lines; and round 7's box, whose stated blocker dissolved when the training dials shipped.

## An item-16 finding, because the audit is what turned it up

Round 29 #16 is filed as a copy ask and it is not one. The tour desk raises **four** notices
(`protocol/offers.ts:95`) and `InboxSheet.subjectOf` (`:120-125`) branches on only two before falling
through to `return 'Entries suspended'` – so the **season** letter (`raiseTourSeasonLetter`,
`offers.ts:1253`, raised at `world/mandatory.ts:262`), whose body is the top-50 mandatory regime,
arrives in his inbox announcing a suspension that has not happened. **A missing branch, not a
wording.** Full working in [round-29.md](round-29.md) #16.

## Where the rot is now

It is not in the ledgers any more; rounds 23–28 are the best-kept files in the folder. It is in the
two places that route work:

1. **`docs/now-next-later.md` (reviewed 27.08) carries three of roughly twenty open items** – round
   16 #8, round 16 #20 and round 8 #1. Everything else in this audit is *recorded* but nobody is
   holding it: morale, R11-1b, the cadence table, the events counter, the two round-17 questions,
   `TierGuide`'s `$0`, round 27 #8. That is the difference between "written down" and "waiting on
   somebody".
2. **`docs/rounds/README.md` has no row for rounds 26, 27 or 28** – the fifth time this table has
   lagged its own folder, and its own closing section already predicts exactly this.

⚠ Round 29 #18 – «добавить в скилл pull-request проверку несделанных пунктов из раунда» – is the
mechanical check both of those need, and it is the right instinct. **This is the third audit; the
first two both ended by saying the fix is a check and not another audit.**
