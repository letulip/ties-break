# Golden saves corpus

One world-shaped JSON payload per historical save schema: `v0.json` … `v{SAVE_SCHEMA_VERSION}.json`.

**Backward compatibility is a hard product guarantee.** A player who last opened the game on any
past version must be able to load their career after an update. `tests/goldenSaves.test.ts` runs
every fixture here through `migrateSave` and asserts it upgrades cleanly to the current schema.

## The rule (enforced by the test)

- There must be exactly one fixture for **every** version from `v0` to the current
  `SAVE_SCHEMA_VERSION` (from `src/engine/world.ts`).
- Bumping `SAVE_SCHEMA_VERSION` therefore **fails the suite until a new `vN.json` is added** — the
  guard forces you to capture a representative save of the shape you just froze.

## How each fixture was reconstructed

Shapes come straight from the append-only history in `src/engine/migrations.ts`:

| file | shape (delta vs the version before) |
| ---- | ----------------------------------- |
| v0 | pre-release dev save: no `schemaVersion`, no `fundsCents`, flat `log` strings |
| v1 | `+schemaVersion +fundsCents +log` |
| v2 | `+profile` (no `playStyle`) |
| v3 | profile `+playStyle` |
| v4 | `+plan` (weekly time split) |
| v5 | `+careerId` (career profiles) |
| v6 | living world: `+cohort +results +season +entries +events +nextEventId +kidRank`, `log` dropped |
| v7 | profile `+kidLastName`, world `+prevKidRank` |
| v8 | world `+pendingTournament` (tournament-reveal flow; `null` when no reveal is in progress) |
| v9 | profile `+birthMonth` (1-12; relative-age-effect groundwork, round-3 QA item 16 / round-6) |
| v10 | world `+bestFinishByTier +lastSeasonSummary +seasonWins +seasonLosses` (Home season strip + SeasonSummaryDialog) |
| v11 | world `+financeWeeks` (per-week/per-category finance ledger; feeds the Money breakdown past the 60-event cap) |
| v12 | world `+condition +injury +injuryHistory +physioActive` (Season-Life availability gate; injury/physio wired for Slice C, condition drives the fatigue caution) |
| v13 | world `+vacations +practices +recoveryBuff` (season planner: booked family-vacation weeks, booked practice-match weeks, and the carry-over injury-tau buff from a resort/elite package) |
| v14 | world `+seasonHistory` (R10-9: append-only per-season record – year, end rank, points, W-L, funds delta, closing funds, best finish – behind the Stats season-by-season table; `lastSeasonSummary` is overwritten yearly, this list is not) |
| v15 | world `+internationalEntryWeeks` (the ITF annual entry cap: the absolute week of every j30/j60/j300 entry she has made, counted per season against `ECONOMY.entryCap.perYearByAge`. Pre-v15 saves backfill to an empty ledger – nothing in them can reconstruct it, since the kid's result row is award-only) |
| v17 | world `+seasonStartRank` (R12-S1: her dense rank as she ENTERED the season in progress, captured at the tick into the season's first week. It is not derivable at wrap-up time – the 52-week prune has removed every result behind it by then, which is how the wrap-up came to replay an almost-empty table, tie the whole field on 0 points and report "from #1". Pre-v17 saves backfill to `null`, the "not recorded" value `SeasonSummary.startRank` has always allowed, and capture a real one from the next season boundary) |
| v16 | `seasonHistory` rows `year` → `+seasonIndex` (a season's identity is its 0-based index, never the calendar year of its first Monday – a season is 364 days, so that year repeats at season 5 and the wrap-up's dedup guard silently dropped a whole season's row). The backfill is an exact inversion: the old guard kept the first season to claim a year, so the smallest index yielding it is the one that wrote it) |
| v18 | world `+milestones` (Diary-1 D10: the durable milestone ledger behind the Memory card – first title/final per tier, first international entry, first injury, each season's closing rank, captured at the moment they happen. Pre-v18 saves backfill from surviving evidence: tournament events + the kept `first-title` event + kid result rows for titles/finals, `injuryHistory` for the first injury (onset = week − weeksOut), `seasonHistory` for season ranks, and the earliest surviving j30+ trace for the first international. Earliest evidence wins per identity; history already pruned away is honestly unrecoverable, so a backfilled "first" is the earliest surviving one) |

| v19 | world `+skills +potential` (Phase 4 development: her build stops being re-derived from `seed:kid` on demand and becomes state that moves, under a ceiling rolled once from `seed:potential`. Pre-v19 saves backfill from that same derivation, so a migrated career is byte-identical at the moment it loads and simply starts developing from there) |
| v20 | cohort rows `+ageYears +potential` (the field gets an age and a ceiling – it used to grow ~1.5 a year for ever, so no career could catch it. Backfilled deterministically from the player's seed, with each rival's ceiling measured from where the old unbounded drift left them, so a long-running save keeps the players it earned) |
| v21 | world `+academy` (the academy scholarship: `{level, sinceWeek, seasonIndex, coveredCents}` or `null`. Pre-v21 saves backfill to `null` rather than replaying the reviews they never had – a replay would have to invent the offer, the renewals and the kit grants, or hand her a scholarship she was never told about. The next season boundary reviews her on the year she just played and makes a real offer) |
| v24 | world `+coachOnEventWeeks` (does the coach travel to tournaments? A competition week stops being billed as a coaching week by default – she spends it in a draw, not on his court – and this buys him for those weeks anyway. It moves BOTH the bill and the development rate, because a coach who is not paid for a week is not at that week, which is what keeps it a decision rather than free money. Back-filled **false**, which is the new default and deliberately NOT a preservation of what the save was doing: an existing career was billed every week, so the migration makes its coach cheaper and, on tournament weeks, absent. The owner's framing is that the automatic rule is "competition weeks are not coach weeks", so that is what a migrated career should wake up under – and it is the cheaper direction, which is the safe one to migrate into) |
| v23 | world `+coachId` (a roster coach's id, or `null` for the parent on the court. The roster is a pure derivation of the seed – `engine/coach.ts buildCoachRoster` – so only the id is stored and it resolves for ever without a further migration. The back-fill hires the coach at the rung the career was already being billed at, choosing by style fit and then by price, which is the same rule `openingCoachId` applies to a brand-new career: the save keeps its tier and its development factor and simply gains a name and a face for the money it was already spending) |
| v25 | world `skills` / `potential` `+groundstrokes` (the fifth attribute – damage off the ground, the leg of a point the model never had. Both numbers come off draws APPENDED to the existing `seed:kid` / `seed:potential` sub-streams, so the four attributes a save already holds are byte-identical and no career's build moves. The back-fill is the interesting half: her birth value would open a week-200 career with a fourteen-year-old's forehand and her ceiling would be the mirror mistake, so she is placed at **the same share of the new axis's headroom that she has taken on the other four** – she has been hitting forehands the whole time, the attribute was not modelled but the girl was. This fixture is the real migration's own output on `v24.json`: that career is 33.8% of the way to its ceiling on all four, so its groundstroke lands 33.8% of the way from 51 to 55.6. A fresh career gets `progress = 0`, i.e. exactly `startingSkills`, so a new game and a migrated week-1 game are still the same career) |
| v26 | world `+knock +knockHistory` (W4 – the knock: the ordinary training week's one event. She comes off court sore, and the parent rests it or sends her back out; see `src/engine/knock.ts`. `knock` is the live one – part, the week it arrived, whether it is a part he has pushed through before, and **his answer** – and `knockHistory` is the retired ones, which is what makes the thread accumulate (`pushedParts` reads it to decide which part of her body speaks up next). **Both back-fill EMPTY, and unlike v18's milestones there is nothing to reconstruct**: a knock left no trace an old save could carry – it was never rolled, never logged, never persisted – so there is no earlier evidence to mine. Inventing rows would mean inventing *decisions the player never made*, and since `knockHistory` is precisely the record of his decisions, a fabricated row would hand a career a weak shoulder whose owner never ignored anything. A migrated career therefore wakes up with a clean body and a clean record, and its first knock arrives on its next ordinary training week like any other. Nothing else in the save moves: the two sub-streams (`seed:knock:<week>`, `seed:knockread:<sinceWeek>`) did not exist before, so no existing stream shifts by a draw, and the injury threshold's new post-draw multiply returns exactly 1 while `knock` is null – which it is for every migrated save at the moment it loads. This fixture is the real migration's own output on `v25.json`, the same recipe v25 used on v24) |
| v28 | world `+seasonRecord` (the season W-L, told apart by ladder – `{domestic: {wins, losses}, itf: {wins, losses}}`. The owner, 31.07: «national/international разделить победы и поражения, мне кажется они не должны быть общими». It is a **decomposition** of the two v10 counters and NOT a replacement for them: `matchesEverPlayed` folds `seasonWins + seasonLosses` into the radar's confidence, which is documented as a count that may only ever go UP, so a total that fell would re-thicken the fog on its own. The back-fill is a real **reconstruction**, and exact wherever the event feed still reaches: `finalizeTournament` is the only writer of a `tournament` event, it stamps `finishIdx`, and a finish index over a known draw size IS the match count – `rounds - finishIdx` wins plus, unless she won the thing, exactly one loss. The tier comes off the summary's label prefix through the shared longest-label-first lookup, the same recipe v10 and v18 use. Only the CURRENT season is read, because the counters reset at every wrap-up and an older row is already banked in `seasonHistory`; a save whose feed has pruned past its season start keeps its true total and gets a split accounting for as much of it as the evidence supports, with the remainder attributed to neither ladder because nothing left in the save says which one it was. Same bargain v17 struck for `seasonStartRank`, and it costs at most the tail of one season on one screen. This fixture is the real migration's own output on `v27.json`, the recipe v25 and v26 used) |
| v30 | world `seasonRecord` `+wta` (the adult rungs, task #17). `LadderTrack` went from two members to three, and `seasonRecord` is the only place in a save where that union is a persisted KEY SET rather than something re-derived on the next tick – every other consumer (`kidRankWta`, `prevKidRankWta`, `ladders.wta`) re-folds from the results ledger and therefore needed no migration at all. Without this step a save written before the professional table existed would carry a two-key object into a three-key type and `record[track].wins++` in `finalizeTournament` would throw on `undefined` the first time she won a W15 match. **The back-fill is uniquely EXACT among the ones in this file**: v18 mined surviving evidence and admitted its "firsts" were only the earliest still-visible ones, v28 reconstructed a split and clamped it because the feed had been pruned, v29 refused to guess at all – this one guesses nothing and loses nothing, because W15/W35/W100 did not exist in any shipped build and so no match in any save reaching this line can have been played on the professional table. Zero is not a placeholder for the true value, it IS the true value, and the invariant `seasonRecord` sums to `seasonWins`/`seasonLosses` still holds afterwards. Written defensively (an absent or non-object `seasonRecord` is rebuilt whole) because append-only means a later step must not assume an earlier one's post-condition still reads the way it did the day it was written. This fixture is the real migration's own output on `v29.json`, the recipe every fixture since v25 uses) |
| v22 | profile `coachSetup` → `coachTier` (the coach ladder: `'parent' \| 'hired'` becomes `'self' \| 'budget' \| 'middle' \| 'high' \| 'elite'`. The back-fill is the owner's ruling and deliberately blunt: `hired` → `elite`, `parent` → `self`. It is also what the arithmetic mostly said – the old `hired` band's ~$475/wk midpoint is what the spec prices an Elite coach at, to within $5 – and both mappings are **development-neutral**: `self` carries 0.82, exactly the old `coachParent`, and `elite` carries 1.15, exactly `coachHired`, so no migrated career's growth rate moves. Only its bill does, and the Coach Market ships in the same wave to let it change) |
| v29 | `seasonHistory` rows `+spentCents +earnedCents` (W7 – what each season COST and BROUGHT IN, gross, in positive cents. The owner: «было бы очень интересно где-то хранить всю историю затрат за карьеру по годам в каком-то виде». `fundsDeltaCents` was already there and answers a different question – whether the year ended up or down – which a season of big prize money and bigger bills reports as a shrug. **The migration is a NO-OP and that is the point**: the figures come off `financeWeeks`, which is pruned to a 60-week trailing window, so every season older than ~14 months has had its per-category rows deleted from the save long before this runs, and the only other trace (`lastSeasonSummary`) holds one season and is overwritten yearly. A back-fill would have to invent numbers, and this is the field where an invented number is worst: the whole feature is a parent asking what she has cost, and a fabricated answer to that is a lie with a dollar sign on it. Both fields are OPTIONAL and the reader prints silence for a row without them – the contract `bestFinish` has carried since v14 – so a migrated career shows its old seasons as not recorded and keeps real figures from its next wrap-up. This fixture is the real migration's own output on `v27.json`, the same recipe v25 and v26 used) |
| v31 | world `+trophiesByTier` (the TITLES LEDGER, behind the Trophy Cabinet: `Record<TierId, {titles: number[], finals: number[]}>`, arrays of the absolute WEEKS she won a tier and the weeks she LOST a final at it. Written in `finalizeTournament` beside the v10 `bestFinishByTier` update. **It is a new fact rather than a view of an old one, because nothing already in a save can answer "how many, and when"**: `bestFinishByTier` is a single high-water mark with no week and no count, and it is overwritten the day a silver becomes a gold; `milestones` is firsts-only by identity (`title:<tier>`, so a five-time J30 champion has one row); `results` prunes at 52 weeks and `events` at 400, of which only 60 reach a Snapshot. ⚠ `finals` here means **she LOST the final** – the opposite of `MilestoneType: 'final'`, which a title also captures – because a cabinet counts pieces of silverware and a week produces exactly one; counted the milestone's way the silver plate would light up the first time she WON something. **The back-fill is a deliberate NO-OP, in v29's sense**: the shape is created, the history is not invented. The evidence is pruned away, and the little that survives would give a confident WRONG answer rather than a partial one – reading `milestones` would print "1 J30 title, in 2033" for a five-time champion, a number with a year on it that never happened. v18 could strike that bargain because a memory that starts late is still a memory; a cabinet prints a COUNT, and the count is the whole point. The owner, asked directly: «нас это в данный момент не беспокоит». So a migrated career wakes up with eighteen empty shelves and its next final is the first thing that goes in one. Written defensively (an absent or non-object value is rebuilt whole) for the append-only reason v30 states, and `finalizeTournament` creates a missing tier's row on demand so a tenth rung on `TIER_LADDER` needs no further migration at all. This fixture is the real migration's own output on `v30.json`, the recipe every fixture since v25 uses) |
| v32 | world `+offers` (THE INBOX: every letter this career has been sent – `{id, kind, week, deadlineWeek, terms, state}` plus the two bookkeeping fields a signed deal needs, see `docs/specs/offers-and-the-inbox.md` §2. This slice carries the KIT SPONSOR alone, on the spec's own build order: it is the smallest step that proves the whole shape – arrival, deadline, sign, refuse, expiry – against a number that is already balanced. **It is on the world rather than in the event feed, and the spec makes that a rule**: a signed deal has to outlive every prune, `events` caps at 400 rows and a busy career burns that in a couple of seasons, so a contract announced in the feed is a contract that silently stops paying for her equipment two seasons later. ⚠ **The back-fill is empty, and unlike v29's and v31's no-ops that is not a bargain struck with a pruned log – there is genuinely nothing to reconstruct.** v18 mined surviving evidence, v28 rebuilt a split from finish indices, v31 declined to guess at a count it could not see; all three were looking at a record of things that HAD HAPPENED. An offer is a record of a DECISION, and no decision was ever taken – until this version the kit deal was not a decision at all but weather (`reviewLocalSponsor` added $1,000 or $2,000 at the season boundary and the player was never asked), so there is no earlier state that means "he signed" or "he refused". This is v26's case exactly: fabricating rows would mean fabricating decisions the player never made, which is the one thing a ledger OF decisions must never contain. ⚠ **And the cash it was receiving simply stops** – the one migration here that leaves a career materially poorer. Nothing is clawed back and every grant already banked stays banked, but the next season boundary raises a letter instead of a cheque and the parent has to sign it; the deal he can sign is worth the same figure in KIT (the shop covers her racquet, string and shoe bills up to the allowance and keeps her kit fresh) that it used to be in money – «кит вместо денег». Written defensively (an absent or non-array value is rebuilt whole) for the append-only reason v30 states. The one sub-stream the slice adds, `seed:offer:<week>`, did not exist before, so no existing stream shifts by a draw and the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v31.json`, the recipe every fixture since v25 uses) |

| v33 | offer `terms +covers +travelShare +seasons` (THE BRAND LADDER: a sponsorship rung stopped being one shop and became three, and **the rung says WHICH OF HER EQUIPMENT LINES the deal covers** – `local` her strings, `national` strings and frames, `global` everything plus a share of the airfare. See `SponsorTier` for the design, `ECONOMY.sponsorship` for every number, and `docs/specs/offers-and-the-inbox.md` §4.1b. The `local` gate is the domestic table it always was; the two rungs above it read the **ITF** one, which is the whole point – the owner finished a season #1 national and #13 international and only a shop in his daughter's town was writing to her. ⚠ **This one BACK-FILLS, which is the opposite of v32 a row above, and the difference is exactly the one v32's own note draws.** v32 refused to mine because an offer is a record of a DECISION and no decision had ever been taken; here a decision HAS been taken – a v32 career may be holding a signed contract this very week – and the three new fields are not a guess about what the player chose, they are the terms of the deal he already agreed to, written down. Refusing to fill them would not be modesty, it would be losing a contract. ⚠ **And the value is the OLD behaviour, not the new rung's.** Under v32 a local deal covered ALL THREE LINES (`KIT_DEAL_CATEGORIES` was rackets/stringing/shoes and the freshness cap applied to every line of `KitWear`); under v33 a local deal covers her strings alone. A back-fill of `TIER_COVERS.local` would quietly take two lines away from a contract whose letter is still in the player's inbox promising them – re-writing history in the brand's favour. `activeKitDeal` has said since the day it shipped that a deal signed under one set of numbers is honoured under those numbers for its whole life; this is that rule meeting its first real test, and `untilWeek` is left alone for the same reason. `travelShare` is 0 and `seasons` is 1 because no rung paid for travel and no deal ran past one season before this version. Written defensively (a malformed terms object is skipped, an already-filled one is left alone) for the append-only reason v30 states, which also makes it idempotent. The arrival draw stays on `seed:offer:<week>` where v32 put it – no stream is added or reordered, so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v32.json`, the recipe every fixture since v25 uses) |

| v34 | world `+onRampCleared` (THE ON-RAMPS SHE HAS ALREADY CROSSED – `{itf, wta}`. An on-ramp is the bottom rung of a table, the one with no acceptance list, and its band is denominated in the table BELOW it: J30 reads her domestic best-6 against 250, W15 reads her ITF junior best-6 against 120. ⚠ **Both of those are ROLLING 52-WEEK windows, so the evidence that she once cleared the bar deletes itself.** A season spent abroad ages out every domestic result and the J30 door closes behind her; from eighteen the J rungs are shut on AGE, so no junior point can ever be earned again and W15 closes on its own a year later with nothing she could do about it. The owner, playing: «не может играть в J серии, потому что ранг в national упал» – and, on the rule: «въезд – это порог, который переходят один раз, а не условие, которое держат постоянно». Measured before the fix (`tools/j30-onramp-lock.ts`, 216 careers): **209 went through the J30 door and were shut out again, 160 of them while J60 or J300 stood OPEN**, median 53 weeks of it; and **188 had at least one week at 18+ with nothing open on either real table**. After: 0, 0 and 7 – and those 7 never cleared the W15 standard at all, which is a girl who did not make it rather than a rule eating her access. ⚠ **Acceptance lists deliberately do NOT latch**: J60/J300/W35/W100 are entry cuts read against a CURRENT ranking, because no real entry list admits you on a ranking you held two years ago. The latch guarantees a way back ONTO a table, never a place in a field. ⚠ **The back-fill is EXACT, which is rare in this file and worth the sentence.** v18 mined surviving evidence and admitted its firsts were only the earliest still-visible ones; v28 clamped a reconstruction to what the feed still reached; v29/v31/v32 refused to guess at all. Here nothing has to be guessed: `bestFinishByTier` is a HIGH-WATER MARK that is never pruned, so "has she ever finished an event on this table" is answerable for every save however long ago it happened – which is the difference between a girl keeping the access she earned and a girl being asked to earn it twice. Two weaker sources are OR-ed in for saves with no marks yet (a counting result inside the current window; the on-ramp's band met today), and `latchOnRamps` re-checks both on every tick anyway, so they only make a migrated save correct BEFORE its first tick. Written defensively and idempotently (an existing object is left alone) for the append-only reason v30 states – and `latchOnRamps` itself is defensive for a sharper version of the same rule: the migration ladder calls `recomputeKidRank` from its EARLY steps, long before this one runs, so the field genuinely is absent there. Pure state, zero draws, no sub-stream added or reordered – the frozen MAIN capture (41550 / e6b0c709) cannot move. This fixture is the real migration's own output on `v33.json`, the recipe every fixture since v25 uses) |

| v35 | world `+rngMain` (THE PERSISTED MAIN POSITION – `{s, n}`, mulberry32's register and the cumulative MAIN draw count, docs/review/proposals/P3-rng-persistence.md. Until this version the stream position was not in the save at all: every load rebuilt it by replaying the whole career – `restoreRng` ran one full `tickWeek` per elapsed week, ~1.5 ms each, on every load of every career, for ever – and the validity of that replay taxed every feature with the frozen-capture discipline (the cross-suite constant 41550 / e6b0c709 that could never move). ⚠ **The migration performs that replay for the LAST time per career and writes the answer down**: `replayMainState` is byte-identical to what `restoreRng` did – a fresh probe world on the same seed, no entries, one tick per week, valid because the per-week MAIN draw count is independent of player input – and its `{s, n}` output is stamped into the save. The two fields are REDUNDANT on purpose (`s = seed32 + n·0x6d2b79f5 mod 2³²`, mulberry32's register advances by a constant, output-independently): the pair is its own checksum, `mainStateConsistent` is the load-time verifier, and a corrupted pair routes through `recoverMainState` – the same replay, now demoted to corruption recovery – with the snapshot's `recovered: true` flag. ⚠ **The invariant regime changed with this version**: input-independence (player choices cannot re-roll the world's dice) is still law, proved pairwise A/B in the converted suites; cross-version draw-count stability is retired – the capture constant in tests/condition.test.ts B1 is a documented measurement now, updated freely by any wave that legitimately adds a MAIN draw. This fixture is the real migration's own output on `v34.json`, the recipe every fixture since v25 uses: seed golden-v27 at week 60 lands on `{s: 1815598547, n: 47942}` – 60 × (3 base + 4×199 drift) + 2 sponsor hits, the arithmetic of the tick's own budget – and that exact pair is the frozen tripwire in tests/migrations.test.ts that catches any future change silently drifting the migration replay) |

| v36 | world `+proEntryWeeks` (W2-LADDER, act2-pro-tour.md §5 – THE PRO AER LEDGER: the absolute week of every W-rung entry she has made, counted per season against `ECONOMY.entryCap.proPerYearByAge` {16 → 12, 17 → 16, 18+ unlimited}. The WTA's own age-eligibility rule – the Capriati rule, which exists for exactly our story – built as `internationalEntryWeeks`' exact PARALLEL and never merged with it: the real rules are "separate from and additional to" each other (ranking-points-by-tier.md §4), so a sixteen-year-old holds BOTH allowances at once – 25 junior entries and 12 professional ones – and spending one never touches the other. Entered at enter-time, spliced on a refunding withdrawal, KEPT on every forfeiting exit (the tour counts participation), pruned to the current season at housekeeping. **The back-fill is an EMPTY ledger, v15's own bargain at its own version, and the argument transfers verbatim**: nothing in an older save can reconstruct the entries this cap exists to count – the kid's result row is award-only (a first-round W15/W35 exit leaves no row at all), `world.entries` prunes to future events the week one is played, and no never-pruned mark records ENTRY (bestFinishByTier records finishes; a walkover week has the one and not the other). So a migrated career resumes with this season's pro allowance untouched – the lenient direction, on purpose; careers at 18+ never feel it at all. The cap ships WITH the boredom guard (owner ruling 2, «игрок должен видеть теннис так или иначе»): tools/boredom-guard.ts is the acceptance bench proving every cap-refused week still offers a playable J or domestic event. Zero draws – the entry-cap family is post-draw gating end to end – so the frozen MAIN capture is untouched by construction. This fixture is the real migration's own output on `v35.json`, the recipe every fixture since v25 uses) |
| v37 | world `+kit` (W3-KIT – **THE QUALITY LADDER THE PLAYER CHOOSES**: `{ grade, sinceWeek }`, a `KitGrade` per `KitLine` {alloy, composite, performance, pro} plus the week she was last handed a new one of that line over the counter. The equipment model has had a CONDITION axis since it shipped and no QUALITY axis at all; the owner ruled it needs both, «как с тренерами», because an aluminium starter frame really is heavier, slower and harder on the arm than a composite one – so equipment now moves BOTH performance and injury, and a rung is a decision, which is the one class of fact this engine never re-derives (hence the first persisted field the equipment model has ever needed). ⚠ **The back-fill is `composite` on every line, and it is the only value that could be correct.** That rung is the ladder's IDENTITY ELEMENT by construction – zero `startWear`, `lifeFactor` 1, `priceFactor` 1 (`ECONOMY.equipment.grades`) – so a migrated career's wear curve, its injury threshold and its gear bills are byte-identical to the week before it was loaded, which is what "old saves open unchanged" has to mean for a field that feeds the match engine. Filling `alloy` would silently make every existing girl worse at everything on load (the exact behavioural surprise the migration rules exist to prevent); filling `pro` would hand her the top of a ladder she never bought and leave a live career with nothing to spend on. ⚠ **And `sinceWeek` is 0, which reads as "never bought one by hand" and is ALSO a no-op**: `kitWearAt` takes the more recent of the scheduled purchase and the hand purchase, and `week - 0 = week` is exactly what `weeksSinceGear` returns before the first scheduled hit, so the minimum is the scheduled clock at every week on every line. ⚠ **The anti-destiny bound survives structurally, not by tuning**: a rung only decides where on the EXISTING wear curve she stands and how long that curve is, and both land inside `kitWearAt`'s `clamp01` – so the whole ladder lives inside [FRESH_KIT, SPENT_KIT] and tools/kit-bench.ts §1's nominal 2.01 < 2.40 skill points is unchanged by the ladder's existence. Written defensively and idempotently (a well-formed rung is never reset, a malformed one is replaced, a missing LINE inside an otherwise valid object is filled) for the append-only reason v30 states. No sub-stream is added or reordered and not one draw is taken – `resolveGear` still bills exactly the cents `seed:gear:<category>` produces and multiplies afterwards – so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v36.json`, the recipe every fixture since v25 uses) |
| v38 | world `+penalties` `+suspendedUntilWeek` (W3-ACT2 – **THE PENALTY LEDGER AND THE TOUR'S OWN SENTENCE**, act2-pro-tour.md §6, the owner's spec as canon: «10 штрафных очков за 52 недели -> отстранение на 4 недели», sources being a skipped mandatory event, a late withdrawal and a no-show, binding the top 50 only. `penalties` is `PenaltyRow[]` – one row per charge, each with the absolute week, the points and the RULE – and `suspendedUntilWeek` is the last week of a suspension or null. ⚠ **Rows and not a running total**, `internationalEntryWeeks`' own argument one table up: the rule is ten points inside a ROLLING 52 weeks, so the total is a filter over the window, a missed reset is impossible by construction, and points age out on their own – which is what makes the regime forgiving in the way the standing ruling requires. ⚠ **The sentence is persisted rather than derived** because it IS a decision taken at a moment: recomputed from today's window it would end early the week its tenth point aged out, so the same career would be suspended or not depending on when you asked – the two-surfaces-disagree class `refreshDerivedRankCaches` exists to close. ⚠ **The back-fill is the identity `[]` / `null`, and here that is the only TRUTHFUL value rather than merely the safe one.** The regime did not exist before this version, so no career has ever been charged anything; inventing history would mean fining a player retroactively for a rule she was never told about, which is precisely what «мы ни за что не наказываем» and §6's «announced before it can bite» forbid. A migrated career wakes up owing nothing and free to play, and the first obligation it meets arrives as a letter like everybody else's. ⚠ **It is not a no-op for a top-50 career, and that is deliberate**: a save loaded here may already be inside the standing the regime binds, so the next mandatory entry DEADLINE it ticks past writes its first warning letter – which is the announcement, and the reason the warning fires at the deadline rather than at the event. Written defensively and idempotently (a well-formed array is left alone, a malformed `suspendedUntilWeek` becomes null) for the append-only reason v30 states. No sub-stream is added or reordered and not one draw is taken – the whole regime is post-draw bookkeeping – so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v37.json`, the recipe every fixture since v25 uses) |

| v39 | world `+ending +debtSinceWeek +careerTotals +fork +retirementOffer +oneMoreYearCount +college` (WHERE THE CAREER ENDS – W2-ENDINGS, career-contract-v1.md §4. Six endings on one surface: she stops at nineteen, college, bankruptcy, the career-ending injury, the natural end from 29 and the plateau reading that lets it ask early. ⚠ **Six of the seven back-fills are the identity and could not have been anything else** – until this version nothing in the game could end, so there is no history to reconstruct and no way to invent one honestly. ⚠ **The seventh, `fork`, had to be a real reading, and refusing to make it would have broken every loaded save.** The fork at nineteen BLOCKS the advance until answered; a career loaded at twenty-four with `fork: null` would be halted on its next tick and asked whether she wants to stop at nineteen, about a decision she visibly made five years ago. She is twenty-four and still playing, so `{ askedWeek: <the week she turned 19>, answer: 'continue' }` is the only truthful row – and a career still under nineteen gets `null` and is asked on the day, like everybody born on v39. The birthday week is SEARCHED rather than computed (`kidAgeYears` folds the birth month into the real calendar, so a December girl does not turn nineteen at week 260). ⚠ **`careerTotals` is exact for a young career and a documented undercount for an old one**, the same bargain v15 struck: `financeWeeks` prunes to a 60-week window and `seasonHistory` keeps a NET delta that cannot be split back into gross in and gross out, so a save past its second season simply does not contain what it spent in its first. The visible consequence is stated rather than hidden – a migrated career's album may show the break-even crossing EARLIER than it truly happened, because the costs behind it were pruned before the counter existed; careers begun on v39 are exact. ⚠ **`debtSinceWeek` RESTARTS the spell** rather than reconstructing it, which is the generous direction on purpose: a family loaded under water gets the full grace window from the week it loads, because the alternative is a career that opens and ends in the same click for weeks the player was never warned about. Pure state, zero draws, no sub-stream added or reordered – the frozen MAIN capture cannot move. This fixture is the real migration's own output on `v38.json`, the recipe every fixture since v25 uses) |
| v40 | `careerTotals` `+weeksLostToInjury` (THE PRUNE-PROOF ACCUMULATOR – docs/specs/fatigue-injury-audit-2026-08.md §6. The career-ending injury (#4) fires on a fresh `severe` landing on a body that has already lost N weeks, and that sum was being taken over `injuryHistory` – a list `rollInjury` PRUNES to its last twenty rows. So the accumulator ran short exactly on the most-injured careers, and the rule got harder the more broken she was: measured over 90 full careers, 13 reached the cap and 1.4% of onsets were judged against a total a mean of 6.1 weeks light. ⚠ **The back-fill is exact under twenty layoffs and an honest undercount above it, and there is no third option** – the pruned rows are gone and nothing else in a save records them (`events` prunes at 400, `milestones` keeps only the FIRST injury, `seasonHistory` has no medical column). Undercounting is the safe direction too: `weeksLostSoFar` takes the LARGER of the counter and the surviving history, so no migrated career can have its ending fire on weeks it never lost. Pure state, zero draws – the frozen MAIN capture cannot move. This fixture is the real migration's own output on `v39.json`)
| v41 | kit offers `+fromWeek`, and every signed `untilWeek` snapped to the season's CONTRACT END (docs/specs/sponsor-window-2026-08.md). The brands' review became a FIVE-WEEK WINDOW – the off-season plus the two weeks before it, the owner's «межсезонье +2» – with a letter landing on each of the first four weeks and a contract ending on week 49 rather than 51, so that the slot is demonstrably empty while the window is still open. Two consequences on disk: a deal signed inside the window can start AFTER the week it was signed (the outgoing contract is still supplying her for up to three of those weeks), so the start of cover is its own field and `decidedWeek` stops doing double duty – the back-fill takes `decidedWeek`, which is exactly what a pre-v41 deal meant by it, so it is EXACT rather than reconstructed; and `untilWeek` is snapped to `floor(w / 52) * 52 + 49`. ⚠ **The snap is DOWN for every shipped shape and UP for anything else.** Pre-v41 terms all ended on offset 51 (`dealUntilWeek` ran `seasons * 52 - 1`; `endDealWithSeason` used `seasonLastWeek`), so migrating trims one or two OFF-SEASON weeks – no tournament, no ranking, no entry, at most a fortnight of the freshness ceiling. A deal ending earlier in the year, which no shipped rule produces, is rounded UP instead: extending a promise is the safe direction and shortening one is not. Pure state, zero draws – the frozen MAIN capture cannot move) |
| v42 | ⚠ **RESERVED FOR A CONCURRENT WAVE, and this fixture is the school wave's placeholder.** The school branch was told to take v43 because another agent may take v42 on a branch that has not landed, and `goldenSaves.test.ts` requires one fixture per version from v0 to `SAVE_SCHEMA_VERSION` – so the rung needs a file whether or not it ever gets a shape of its own. This is a real career at week 300 (past the leaving September at 242) **with the `school` milestone removed**, so loading it is what EXERCISES the v43 back-fill rather than merely surviving it. If the v42 wave lands, its own shape replaces this file's delta and the career stays a perfectly good v42 payload |
| v43 | world `milestones` may carry `{ type: 'school', week }` – the last day of school, captured the week it happens (docs/specs/school-ends-2026-08.md). ⚠ **The FACT that school has ended needs no migration at all**: `schoolIsOver(week, birthMonth)` is a pure function of two numbers every save has always carried, so a career loaded at twenty-two is out of school the moment it is read. What the migration back-fills is the MOMENT – for a career already past `schoolEndWeek` the capture never fired, and without it the album's scroll has a hole where a life changed. The week is `schoolEndWeek(profile.birthMonth)`, the same function the tick calls, so a back-filled row is byte-identical to a captured one and `milestoneKey` (identity = the type) makes the back-fill idempotent. ⚠ **The milestone only, never the feed line**: `world.events` is a news feed pruned to 400 rows oldest-first, so a row dated three seasons ago is either already past the horizon or an old headline in a current feed. Pure state, zero draws – the frozen MAIN capture cannot move) |

| v44 | `WorldEventCategory` `+facility` – the court half of the weekly training bill, split off `coaching` (docs/specs/split-the-bill-2026-08.md). ⚠ **There is no field to migrate and the step is deliberately empty.** A new member of the category union is a protocol change, so the version moves; what it changes is what the NEXT tick writes, not what a save holds. ⚠ **And it back-fills nothing, on purpose**: a v43 career's `coaching` rows are the numbers that were actually charged as one line, and nothing in a save can say which cents of them were the court – the split needs the hourly rate and the hours of the week it was drawn in, and `financeWeeks` keeps a total per category and no rate, no hours and no plan. A reconstruction would be a guess wearing a ledger's clothes, so history stays as it was billed and the split begins at the load week. `byCategory` is a Partial record, so an absent `facility` key reads as absent rather than as zero. This fixture is a real career at week 160 on the split engine – 60 finance weeks all carrying a `facility` row, and coaching and facility rows interleaved in `events` – so it is the shape rather than merely the version. Pure state, zero draws – the frozen MAIN capture cannot move |
| v45 | world `+seasonEntries` – the season's entry ledger behind the wrap-up's new line (docs/specs/season-mirror-2026-08.md). It counts, AT THE MOMENT EACH ENTRY IS COMMITTED, how many of the season's tournaments were entered at a rung she had already climbed past whose title could not have moved her on the table she was ranked on. ⚠ **It cannot be back-filled and the migration does not pretend to.** The judgement is about the best-N book she held ON THE WEEK SHE ENTERED, and `pruneResults` keeps only `world.week - r.week <= 52` – so the book behind an entry made in week 3 of a season being wrapped in week 49 is gone entirely. That is the same 49-week hole `seasonStartRank` (v17) exists because of. So the ledger opens AT THE LOAD WEEK (`{ fromWeek: save.week, entered: [], closed: [] }`) and claims nothing about the weeks before it; the wrap's own test is `fromWeek <= yearStart`, so a career migrated mid-season shows NO line for the season in progress and a real one from the next wrap on. ⚠ **A zero would have been the wrong silence** – «0 could not move her ranking» is the good news, and printing it over a season nobody counted is the defect that reported «no tournaments played» over a 44-19 record. ⚠ **Ids and not two counters**: the count follows the fee, so a refunding withdrawal un-counts its entry – and an entry taken in week 45 can be withdrawn after the wrap has banked and reset, which two bare integers would debit from the wrong season. This fixture is a real career at week 112 – a mid-season ledger with nine entries in it, two of them counted, and a `lastSeasonSummary.entryMirror` banked by the week-101 wrap – so it is the shape rather than merely the version. Pure state, zero draws – the frozen MAIN capture cannot move |

| v46 | `seasonHistory[].byTrack` – a finished season TOLD APART BY TABLE: `Record<LadderTrack, { endRank?, points, wins, losses }>`, banked at the wrap-up (docs/specs/season-by-table-2026-08.md). The owner reported twice that «Season by season в stats в разных вкладках всё ещё одно и то же показывает», and the screen was never the defect: a row held ONE `endRank` (the ITF one – the wrap writes `world.kidRank`) and three folds, so the three tabs had nothing to differ by. It is `seasonRecord`'s shape widened rather than a second convention, so the finished season and the season in progress are read with the same keys. ⚠ **The migration back-fills NOTHING and the step is a bump**, v44's shape for v44's reason: `pruneResults` keeps a rolling 52 weeks, so the results that produced an old season were deleted long before the question was asked – the same 49-week hole `seasonStartRank` (v17) and the season mirror (v45) exist because of. An old row therefore shows its stored `endRank` under INTERNATIONAL (that number always was the ITF rank), no rank at all under the other two, and its points/W-L marked as the fold they are; **a blank means "not recorded" and a zero would mean "she scored nothing"**, which is why `byTrack` is left absent rather than written as three zeroed rows. ⚠ **`endRank` is optional inside the row too**, and for the neighbouring reason: absent = she held no counting result in that table, which is not a place – `rankIn` would have returned the tie floor every member of a zero-point tie shares. This fixture is **the real migration's own output on `v45.json`, walked forward through one more wrap-up on the new engine**, so it carries BOTH shapes at once: two rows banked before v46 (no `byTrack`) and season 2 banked after it, with all three tables non-zero – including a professional row that has 6 points and NO rank, because `rankableTotal` refuses a W ranking until the WTA's minimum activity is met. Pure state, zero draws – the frozen MAIN capture cannot move |

| v47 | `plan.week` – SEVEN DAYS OF SESSION KINDS, `SessionKind[][]` Monday..Sunday, each day holding the kinds she trains that day and an empty array meaning a day off (docs/specs/training-dials.md §10). The owner: «у нас есть расписание недели и на каждый день там идут разные тренировки – это и есть ручки». `train`/`rest` are KEPT and become a PROJECTION of it (4/5/6 sessions -> 60/75/85), so all four engine readers of `plan.train` are byte-identical and `setPlan` is the only writer of either. ⚠ **This is the one migration in the corpus whose answer is exact rather than honest-but-lossy, and `general` exists so that it can be**: every shipped career has been running one number – `growWeek` grows all five skills at one shared rate off one shared luck draw – which is precisely what a week of `general` sessions means, so the step rebuilds the week out of the display conventions the Calendar has been drawing all along (`sessionsForPlan` sessions, placed by `sessionDays`, every one of them `general`) and `aimWeights` returns the all-ones vector for it EXACTLY (integer aim units, not floating-point luck). Same session count, same rate, same bill, same knock chance, same recovery, byte-identical skills. ⚠ **The drawn gym day migrates to `general`, not `fitness`** – the gym day has never been simulated, so promoting it would be the migration changing his game; a loaded career opens with no gym day ticked. ⚠ **And it does not clamp to 4..6 sessions**: a save poked to `train: 100` draws seven session days today and both readers clamp on their own, so the band is enforced on new PLAYER input in `setPlan` and never retroactively. ⚠ **What is NOT identical, and it is ruled rather than implied**: a migrated week is never DOUBLED, and since v47 `summerLoadFactor` follows the doubling instead of the calendar (owner, 10.08: «да»), so school-free weeks come back at 1.0 instead of the automatic 1.4 until he ticks a second session onto a day – measured in docs/specs/school-ends-2026-08.md §7. This fixture is **the real migration's own output on `v46.json`, then given a week a v47 build would have written** – six sessions, four kinds, one doubled Tuesday – so it freezes the shape rather than only the migration's default. Pure state, zero draws – the frozen MAIN capture cannot move |
| v48 | `birthdays` – ONE ROW PER BIRTHDAY: `{week, age, asked, given}`, oldest first (docs/specs/birthday-and-gifts.md §2b). The owner, 11.08: «День рождения как-то незаметно проходит… Важный момент, всё-таки». The popup fires on her birthday week and BLOCKS the advance, exactly as a knock does, because he asked for it to fire ALWAYS – and that is what forces "nothing" to be an explicit button rather than a dismissal. The row IS the answered flag, so a reload can never land with a boolean and a record disagreeing about whether he was asked. ⚠ **The migration is a pure default, `[]`, and that means "no birthdays recorded" – NOT "gave nothing every year"**: a v47 career HAD birthdays (the feed said «She is sixteen this week» every year, and `birthdayWeek` can name every one of them exactly), so walking the calendar and writing a row per year with `given: null` would have been easy and would have stated that this parent gave his daughter nothing on every birthday of her life. He was never asked. Absent is not zero – the same distinction v45 and v46 were built around. ⚠ **The four college years are absent for the same reason**: `resumeFromCollege` spends them in one call with nobody able to answer, so no birthday is raised inside the freeze and none is recorded. ⚠ **And nothing is priced** – the owner: «про цену момент, давай не будем это учитывать в нашем кошельке вообще»; there is no cents field on the gift, on the record or on the wire, and a ship-rule test asserts the same seed through every option ends the season on identical `fundsCents`. This fixture is `v47.json` given TWO REAL ROWS – one present she asked for at 14, one day-together at 15 – so it freezes the shape with content rather than as an empty list (the default is already covered by every fixture below it). The ask rides a purpose-scoped `seed:birthday:<age>` sub-stream and persists nothing, so the frozen MAIN capture cannot move |

| v49 | world `+coachOnJuniorEvents` – DOES THE COACH GO TO THE RUNGS THAT PAY HER NOTHING TOO (docs/specs/coach-travel-2026-08.md §5, the ⛔ finding). The v24 travel stance buys the second seat on the rungs with prize money; until this version the fare simply REFUSED every junior and domestic one, which was the engine deciding for the family. The owner overturned that on 15.08 – «делаем тогда» – with his own model of whose decision it is: «По мне игрок сам решает: есть деньги - едет тренер, нет - не едет, или едет, но быстрее банкротится.» ⚠ **The back-fill is `false`, and it is a PRESERVATION rather than a default chosen for anybody**: every career written before v49 was played under an engine where the junior fare did not exist, so `false` is exactly what it has been doing since the week it was saved – same fares, same rungs, same ledger, and the same match-strength helping (which is handed `coachTravelFareFor(...) > 0` and therefore cannot drift from the fare). Nothing here reads the career's wealth, age or results to guess an answer; it was never asked, and the next visit to screen T finds an unticked box. ⚠ **A second field and NOT a retyping of `coachOnEventWeeks`**, which is the interesting half. One scope field with three values (`none | w-series | all`) is tidier on paper and would have rewritten a boolean persisted since v24 plus every reader of it – for something that genuinely IS a second, more expensive choice on top of the first, and is drawn that way (a nested row on screen T, meaningful only while the first switch is on). Optional on the type for the reason `kit` is: absence is the identity element, read `?? false` at the ONE place it is consulted (`coachTravelFareFor`). ⚠ **And the engine refuses nothing** – the measured consequence (8/30 wealthy·elite and 15/30 middle·middle careers bankrupt, every one in the junior years) is warned about on screen before the first fare is charged and then honoured, because the owner has ruled that outcome is the player's own. This fixture is the real migration's own output on `v48.json` **with both stances ticked ON**, so it freezes the shape with content – a week-155 career on a scholarship, employing a coach, sending him everywhere. Pure state, zero draws – the frozen MAIN capture cannot move |

| v50 | `CollegeState` `+years: CollegeYear[]`, `+pendingCallUp` – THE FOUR-YEAR SKIP BECOMES FOUR YEARS SHE LIVES THROUGH (docs/specs/college-as-a-second-act-2026-08.md). Until this version `resumeFromCollege` spent 208 weeks in one call and the epilogue asserted «Four years, a degree and no ranking at all» – a sentence nobody had measured. It is one year at a time now, because the sport's own case is one year and not four (Diana Shnaider left NC State after about a season and is inside the WTA top 15), and each year is a ROW: the two ends of her skill and her rank, what the family's balance did, and the national-team week if the letter came. ⚠ **The rows cannot be back-filled and the migration does not pretend to.** A v49 career mid-freeze has already lived some of those weeks and there is no honest way to say what they held – the measurements were never taken, `pruneResults` deletes a result 52 weeks after it happened and `financeWeeks` keeps a 60-week window, so the evidence is gone. The same 49-week hole v45 and v46 exist because of. Her ledger therefore opens EMPTY and the next year she spends is her first ROW, not her first year; fabricating rows from the span would have put invented numbers on the one screen this phase exists to make honest. ⚠ **And her SPAN is untouched, which is what keeps the step safe**: `untilWeek` still says when the scholarship ends and `inCollege` still reads it, so all six of the freeze's guards answer exactly as they did. ⚠ `pendingCallUp` **lives inside `CollegeState` and not on the world**, because it is meaningless outside the freeze and a top-level field would have to be nulled by every other path that ends a career. This fixture is the real migration's own output on `v49.json` **walked to her twentieth birthday with one college year banked in it** – a call-up she played two rubbers of and won one, a nation that finished 11th of 14, and the ending re-latched at «1 of 4 years on the scholarship» – so it freezes the shape with content rather than as an empty list. The call-up rides a purpose-scoped `seed:callup:<week>` sub-stream and persists nothing, so the frozen MAIN capture cannot move |
| v51 | `ForkState` `+offer: CollegeOffer \| null` – THE COLLEGE ANSWER GETS A PRICE (docs/specs/what-the-college-place-costs-2026-08.md). Until this version the third answer at the fork was offered **unconditionally and free in 100% of careers**, and the four years cost the family nothing at all: P6 decomposed the college arm banking $152,243 against the tour's $45,544 as *100% avoided spend, scholarship $0*. `docs/research/college-and-the-junior-exit.md` §1d prices a real year at **$30,990** in-state / **$50,920** out-of-state `[S]` and quotes the NCAA's own page – *"Most scholarships are partial"* – so neither half of "free, always" was a model of the thing. The offer is measured **once**, the week the fork is raised, off her JUNIOR record alone (`bestFinishByTier` on j30/j60/j300) plus a `seed:collegeoffer:<week>` sub-stream, and it carries four numbers: which programme saw her, the merit-only `athleticShare`, the means-tested `needShare` beside it, and the family's bill. ⚠ **It is not a gate and cannot become one.** `answerFork` still refuses nothing and the card still draws three answers; the worst offer is `programme: null`, which enrols her as a walk-on at the full price. The measure carries no professional rank, finish or prize money at all, so the rule the owner deleted on 16.08 cannot be re-created from either side. ⚠ **The migration back-fills `null` and invents no offer** – v50's own discipline. `null` reads as *never measured*, not *refused*: such a career keeps the free ride it was shown and `resolveCollegeBill` charges it nothing. This fixture is `v50.json` with the offer the engine actually computes for that career (a **small** programme, a 27.1% athletics award, a 10% need-based layer on a middle US family, **$19,490 a year** left to pay) so the shape is frozen with real content rather than a literal. Zero draws on MAIN either way, so the frozen capture (41550 / e6b0c709) is untouched |
| v52 | `ForkState.offer` becomes a **CHOICE**: `{ quotes: CollegeQuote[]; chosen: CollegeTier \| null; canPayPerYearCents: number \| null }` – A COLLEGE TIER STOPS BEING A FUNDING SHARE AND BECOMES A PLACE WITH A PRICE (docs/specs/the-college-choice-2026-08.md). v51's `programme: 'strong'\|'solid'\|'small'` were **funding shares** (0.85 / 0.55 / 0.30) **derived from her junior record**, over a price that was the same at all three – so the PLAYER chose nothing, and the card printed a residual ($8,673) under a sourced sticker ($30,990) with no line connecting them. The owner's scheme of 17.08 is three PLACES on the three sourced College Board stickers (**$30,990 · $50,920 · $65,470** `[S]`), a merit-only award that is a share of the price of **the place she chose**, and the family paying the rest in 52 weekly payments. ⚠ **The migration rebuilds ONE quote, not three**, and that is v51's own invents-nothing discipline: a v51 career was quoted exactly one price and may be weeks into paying it, so the tier is read off the PRICE ($30,990 → `state`, $50,920 → `national`), the shares and the bill are copied across unchanged, `chosen` is that tier iff she took the college answer, and `canPayPerYearCents` is **`null` = never measured** because v51 never asked whether the family could afford it. **The weekly debit is byte-identical across the move.** This fixture is `v51.json` converted exactly that way. Zero draws on any stream – it writes literals – so the frozen capture (41550 / e6b0c709) is untouched |
| v53 | world `+fieldSeasonPoints` – THE FIELD'S OWN SEASON. Points each professional has EARNED since January, by id. It exists because the professional table was a pure function of (seed, season) and so nothing that happened on court could move it – the owner, playing: «таблица просто "стоит"… и номер 1 мы обыгрывали на шлеме». `runAiTournament` had computed every field pro's finish and then dropped the row to save bytes, which also dropped the RESULT. ⚠ A TALLY, NOT ROWS: ~6,048 finisher rows a season against 1,600 numbers (~3 KB), so the byte objection that justified the drop is still honoured. ⚠ The back-fill is EMPTY and that is a PRESERVATION: every pre-v53 career was played on an engine that discarded these results, so an empty tally is exactly what those seasons contained – inventing one would rewrite old standings against results the save never had. This fixture carries three scoring pros so the shape is frozen WITH content. |

| v54 | world `+kidFundsCents` – HER OWN BANK ACCOUNT (round-23 #18). The owner: «после появления её счета в банке в 18 начать ей призовые переводить какие-то суммы, например начать с 10-20% и может быть наращивать год к году», widened on his own follow-up to «не до 30, а до 40 или 50 вообще, это всё-таки ее карьера?». From her eighteenth birthday `finalizeTournament` splits every cheque: `ECONOMY.kidShare` is the ramp (**10% at 18, +5 points a birthday, 50% from 26**), the family is credited the remainder and she is credited the rest, so the money genuinely LEAVES the family wallet. ⚠ **The back-fill is 0, and 0 is the true value rather than a placeholder** – v30's case, not v29's: no shipped build ever transferred her a cent, so there is no history being declined, there is no history. Nothing is clawed back from `fundsCents`; a migrated career starts splitting at its next cheque. ⚠ **And it could not be reconstructed even if it should be**: `financeWeeks` prunes at 60 weeks and `results` at 52, so a 26-year-old's save holds no trace of the cheques she was paid at 19, and the ramp is a function of her age at each one. Defensive and idempotent (a non-numeric value is rewritten whole) for the append-only reason v30 states. It writes a literal – zero draws on any stream, so the frozen capture (41550 / e6b0c709) is untouched. This fixture is the real migration's own output on `v53.json`, the recipe every fixture since v25 uses |
| v55 | **A REPAIR, NOT A SHAPE** – no field is added, removed or renamed. `pendingTournament` is cleared on load when the reveal's event is no longer on the calendar (round 24, the freeze's hygiene). Until this wave an entry that was still outstanding when the college fork was answered was PLAYED inside the freeze: `tickWeek` stashed a reveal the epilogue screen had no surface to answer, and from that week the tick skipped its whole housekeeping step – no `ensureSeason`, no `pruneResults`, no rank recompute – for as long as the freeze lasted. The owner's own w474 save came out of four years with **0 calendar events, 1 result row and a 200-row junior table tied at #1 on zero points**. ⚠ **The state seals itself, which is why a migration is the only door back in**: `pendingView` returns `undefined` when `eventById` misses, so `snapshot.pending` is null, so `TournamentFlow` never mounts, the sticky bar never draws its resume button, and `advanceWeeks` returns `'tournament'` having ticked nothing – with no toast, because `'tournament'` is deliberately absent from `STOP_REASON_TEXT`. Pressing Play does nothing and says nothing, for ever. ⚠ **The predicate is the mechanism, not the symptom**: "the event is not on the calendar" is exactly `pendingView`'s own refusal. A college-shaped test would be too wide (it would discard a playable reveal whose event IS still scheduled) and too narrow (a future route to the same seal would go unrepaired). ⚠ **Nothing is lost either way** – a FINISHED reveal was already awarded by `finalizeTournament`, so this is the Continue she never got to press; an UNFINISHED one had awarded nothing. ⚠ **And it writes no news**: a migration inventing a feed row would put words in a career's mouth about weeks it did not live. The world heals itself – two ticks rebuild the calendar and one season restores the table. This fixture is `v54.json` at the new version, since the shape is unchanged; the repair itself is driven in `tests/college-freeze.test.ts` against a real v54 payload with the footprint injected. Zero draws on any stream, so the frozen capture (41550 / e6b0c709) is untouched |
| v56 | world `college` `+pendingLeague`, and every banked `college.years[*]` `+league` (round 24 – the student championship, the owner's «как минимум 1 турнир в год колледжа был»). A college year now holds ONE guaranteed, watchable tournament – a draw of eight played through `simulateMatch` on `seed:collegeleague:<week>` – and the national call-up is no longer a bare 40% roll: it reads `NATIONAL_TEAM.callChanceByLeague` off how far she went in it. ⚠ **Null is the true value, not a placeholder** – v30's case exactly. A career already inside the freeze lived its banked years before this fixture existed, so it played no championship in them; a run is three seeded matches against a drawn field and reconstructing one would write a scoreline into a week that did not have one. ⚠ **What it costs such a career, stated**: at most ONE call-up. The letter reads `lastLeagueRun`, and a migrated career has no championship on record until its next `COLLEGE_LEAGUE.seasonWeek` (week 12 of the season) – so a remaining year whose week 14 arrives first gets no letter, and every year after it is unaffected. Nothing else changes meaning: `callUp`, the banked ranks, skills and money are untouched, and a career that has already LEFT college (`doneWeek !== null`) is unaffected in every respect. Defensive (a missing or wrongly-typed value is written whole) and therefore idempotent, and a save with no `college` at all – every career that never reached the fork – falls straight through byte-identical. It writes literals: zero draws on any stream, so the frozen capture (41550 / e6b0c709) is untouched. This fixture is the real migration's own output on `v55.json`, the recipe every fixture since v25 uses |
| v57 | world `college` `+pendingYearStart` (round 24 – her college birthdays happen, the owner's «да, день рождения делай»). `resumeFromCollege` now PAUSES on her birthday week – the loop breaks, the college latch goes back on with the SAME year's end under it, the gift dialog renders over the live Home shell, and the next press finishes the year. That press has to bank the year against the measurements taken when it OPENED (her skill, her rank, the family balance – all history by then), so a paused year persists its opening: `pendingYearStart = { week, skill, rank, fundsCents }`, non-null exactly while a year is mid-flight and cleared by `bankCollegeYear` beside `pendingCallUp`/`pendingLeague`. ⚠ **Null is the true value for every v56 save, by construction** – a college year was spent in ONE worker command until this version, so no v56 save can rest mid-year; every save inside the freeze stands at an academic boundary. ⚠ **What an old save gets, stated**: its LIVED college birthdays stay ABSENT from `world.birthdays` – no rows invented, no dialogs for weeks that already ticked (`pendingBirthday` only ever asks about the current week), and nothing billed because a gift has never cost a cent anywhere (spec §0). The one dialog it can meet on load is its own RESTING week's, if that boundary happens to be her birthday week – the same popup the v48 migration's note calls "exactly right" for a tour save resting on one. Every college birthday still ahead of it pauses the year and is asked properly. Idempotent, writes a literal: zero draws on any stream, so the frozen capture (41550 / e6b0c709) is untouched. This fixture is the real migration's own output on `v56.json`, the recipe every fixture since v25 uses |

| v58 | fork `+departsWeek` (round 24 #5 – the fork moves off her birthday, the owner's «пункт 5 запускай как обсудили»). The ask fires at `schoolEndWeek` now (age 18.0–18.9, the game's one "school is over" fact), the college answer RESERVES a place – the chosen quote is locked and `departsWeek = nextAcademicYearStart(answer week)`, the next 1 September – and `resolveCollegeDeparture` enrols her there: `world.college` is built, B1's entry release fires (full-refund rung, past-deadline exemption, desk letter – it moved from the answer to the departure because an entry made while she still plays the gap year IS a commitment she made), and the college ending latches. Between answer and departure the career is a HOLD that plays: every freeze gate reads `inCollege`, which is false until enrolment. ⚠ **Null is the true value for every v57 save, by construction**: the birthday-era answer enrolled in the same command, so no v57 save can hold an answered-but-not-departed fork – a college fork is either open (no departure booked; answering on new code books a real one) or already enrolled (`world.college` non-null – nothing reads `departsWeek` behind an enrolment, and the career continues byte-for-byte: same latch, same `resumeFromCollege`). 'continue'/'stop' forks never consult it. Idempotent, writes a literal, zero draws – the frozen capture (41550 / e6b0c709) is untouched. This fixture is the real migration's own output on `v57.json`, the recipe every fixture since v25 uses |
| v59 | world `+masseurHired` (the travelling team step 1 – owner round 23 item 9, ruling Б; docs/specs/the-masseur-2026-08.md). The first staff seat beyond the coach: pro-career gated (`activeLadderOf === 'wta'`, the one-way door), hired/fired like the coach, a FLAT weekly salary under the new 'staff' ledger category, suspended – not cancelled – at college and on booked family weeks (the coach's own stand-down pair). His body effect: +1 condition on worked weeks, and the rehab cadence that takes one week off an active layoff every third rehab week, receipted in the feed. ⚠ **False is the true value for every v58 save, by construction** – the seat did not exist, so no earlier career can have hired him; the hire stays a decision on screen T, never a migration's gift. ⚠ `injuryHistory` rows MAY now carry `weeksSaved` (and `weeksOut` records the weeks she was ACTUALLY out): written only from the week a hired masseur first saves one, so nothing is back-filled and absent reads as 0 everywhere. Idempotent, writes a literal, zero draws – the frozen capture (41550 / e6b0c709) is untouched. This fixture is the real migration's own output on `v58.json`, the recipe every fixture since v25 uses |
| v60 | world `college` `+leagueReveal` (round 26 #6 – the College League is WALKED and not reported; the owner, having asked once already: «За первый год в колледже турнир был, но опять сообщили только постфактум, в чем проблема использовать наш флоу турниров полностью и дать возможность игроку их смотреть и сопереживать? Я уже просил это сделать»). Round 25 played the matches and wrote the rows; the parent was handed a summary line forty weeks after the fact, because `resumeFromCollege` reported the week and kept ticking. The championship week now STOPS the year – the same pause her birthday already uses – and `TournamentFlow` walks it: the splash, the pre-match card, the live viewer, the box score and the finale, reached through the very same `tournamentReveal` / `tournamentSkip` / `tournamentClose` commands a tour reveal uses. `leagueReveal = { week, revealed }` is where he is in that walk. ⚠ **Two numbers and no record**: the matches are already `keep: true` rows in `world.events`, so the reveal is a CURSOR over them and copies nothing – which is also why the rows survive a flow that is never opened (round 26 #7's own lesson). ⚠ **It is not `world.pendingTournament`, deliberately**: every payout in this game is reached through `TIERS[event.tier]`, and a student field has no rung – so round 24's `COLLEGE_REVEAL_REFUSAL` still guards a state that cannot occur inside the freeze, and round 25's amateur line (no ranking points, no prize money) is held by the state's SHAPE rather than by a branch. ⚠ **Null is the true value for every v59 save and nothing is back-filled**: a championship already lived is history, and opening a reveal over one would be the game asking him to watch a match it told him the result of months ago, with the year's stop standing in front of the career until he did. His own w502 save migrates to exactly this – four banked years, `leagueReveal: null`, all eight championship matches still openable from the feed. The NEXT championship a career reaches gets the flow, because `resolveCollegeLeague` opens the reveal when it plays the fixture. Idempotent, writes a literal, zero draws – the frozen capture (41550 / e6b0c709) is untouched. This fixture is the real migration's own output on `v59.json`, the recipe every fixture since v25 uses |

For **v0–v5** the migration regenerates the world systems (cohort/season) deterministically from the
seed, so those fixtures are the authentic minimal historical shapes. For **v6–v7** the `cohort` /
| v61 | `fork.offer.quotes[].open` **REMOVED** – the first DELETION in this ladder rather than an addition (round 26 #2, second pass). The owner, having asked twice why the cheapest college place was refused: «по-моему в каждой стране есть домашний универ». The boolean was false on exactly one rule – the in-state price IS US residence – and that rule shut the cheap rung in **23 of the 24 playable countries**, on a choice made at onboarding some 440 weeks before the fork card draws and never mentioned there. He overruled the RULE, so the field goes with it: an always-true boolean would leave the next reader believing a place can be shut and the next edit able to shut one, which is the whole reason this is a SHAPE change and not a value fix. ⚠ **And the migration is not cosmetic.** A v60 save sitting on an UNANSWERED fork carries `state: {open: false}`, and `answerFork` filtered on it – so under new code the card would draw the home row pressable, the player would press «The university at home», the filter would miss and the engine would quietly enrol her at the next place up, $20,000 a year dearer, on the exact screen the round exists to fix. Deleting the key here and deleting the filter in `answerFork` are one fix in two places. ⚠ **An answered fork is unaffected either way** (`offer.chosen` is set and the bill reads the chosen quote; nothing behind the answer ever consulted `open`) and the delete still runs over its quotes, because a field removed from the type must not survive in the payload as a value nothing means. ⚠ **Nothing is re-priced**: `costPerYearCents`, `athleticShare`, `needShare` and `familyPerYearCents` are untouched on every quote – `ForkState.offer`'s own doctrine that a later re-tune may not silently re-price a career halfway through a bill it had already accepted. What a migrated career gains is a row it can press, at the price the fork already quoted it. Idempotent (`delete` on an absent key is a no-op) and it writes no value at all: zero draws, so the frozen capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v60.json`, the recipe every fixture since v25 uses |
| v62 | world `+peakPhysical` – THE BEST HER BODY HAS EVER BEEN, as one number (the long goodbye step 1, `docs/specs/the-long-goodbye-2026-08.md` §3b). A running maximum of `physicalMean(skills)` – the mean of the four attributes `declineFactor` erodes, which is every key except `composure` – kept by the growth phase on the line after `growWeek`, the engine's only writer of `world.skills`. Written every tick and **read by nothing yet**: step 2 is what moves the last retirement offer off her 38th birthday and onto a share of THIS number, so that a body kept well plays to 41 and a wrecked one finishes early. ⚠ **A scalar mean is EXACT here rather than a simplification** – decline is proportional per attribute (`decline * skills[k]`), so every physical attribute keeps the same share of its own peak and the mean's share is that same share; `physicalMean` carries the argument so the next reader does not have to assume it is a fudge. ⚠ **And the back-fill is RECONSTRUCTED, not defaulted, which is the whole interest of this migration.** Seeding today's mean would tell a 38-year-old she stands at 100% of her peak – she would load with nine seasons of decline handed back and be offered «one more year» for another decade. So the block runs `growWeek`'s own arithmetic backwards: past `declineStart` the gain term is zero and the loss is proportional, so the mean is multiplied by `(1 - declineFactor(age))` every week; dividing today's mean by that product returns the value she carried into the decline, which IS the career maximum (before the decline the physical mean is non-decreasing). A migrated career therefore reads exactly the share its age implies – measured off the shipped curve, **89.2% at 33, 68.9% at 38, 55.7% at 41**, identical on three careers with deliberately different ceilings – the same as a career played from scratch, so step 2's threshold will fire at the same age on both. ⚠⚠ Those are NOT the spec's numbers: `the-long-goodbye-2026-08.md` §1's share column reads 87 / 66 / 53 at those ages, 2.3–3.2 points low against the `declineFactor` it says it was measured off; §3a's dial table (70% → 38, 55% → 41) is the half that does reproduce, so the owner's 55% ruling stands. Two bounds are named rather than hidden: the `floor: 20` clamp makes a deep-forties reading slightly high, and a career whose ending has latched keeps the peak it stopped at, which is the right answer for a career that is over. Idempotent (a non-numeric value is rewritten whole), zero draws on any stream – the frozen capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v61.json`, the recipe every fixture since v25 uses; that career is 19.9 and has never declined, so its seeded peak is today's mean exactly – the reconstruction arm is driven on a walked career in `tests/migrations.test.ts` instead |
| v63 | world `+assets` – WHAT THE FAMILY OWNS THAT IS NOT TENNIS (the shop, slice 1, `docs/specs/the-shop-2026-08.md` §5). One array of `{id, boughtWeek, paidCents, valueCents}`, and it **back-fills EMPTY**. ⚠ **Unlike v62 there is nothing to reconstruct, and that is the end of the question rather than the start of it**: a career that reached this build could not have bought anything – there was no shelf, no command, no ledger row and no `'shop'` category – so no earlier evidence exists to mine, and an invented row would hand a family a car nobody chose. This is v26's `knock` case, not v62's `peakPhysical` one. ⚠ **The CATALOGUE is deliberately not part of this**: `ECONOMY.shop.catalogue` is a constant, so the shelf a migrated career sees is the shelf every career sees, and slices 2-7 can add a rung without a second migration for the shop's contents – only OWNERSHIP persists. ⚠ **And a migrated career plays identically**, which is the slice's own acceptance measured rather than hoped: an empty `assets` makes `revalueAssets` a no-op loop, no ledger row is written, no category appears in `financeWeeks`, and the shelf itself is invisible until `activeLadderOf` says professional. `tests/coach-travel-edge.test.ts` proves it as an identity – dropping the one key and rolling the number back to 62 reproduces all three frozen career hashes byte for byte. Idempotent (a non-array value is rewritten whole – v30's rule), zero draws on any stream, so the frozen capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v62.json`, the recipe every fixture since v25 uses |
| v64 | `college` `+callUpReveal` – THE NATIONS CUP TIE'S REVEAL (round 27 #6, the owner: «И опять на те же грабли … матчи только постфактум … проводить этот турнир по обычному флоу турнира»). One optional field beside v60's `leagueReveal`, `{week, revealed}`, and it **back-fills NULL**. ⚠ **Null is the TRUE value here, not a placeholder, and for a sharper reason than v31's empty shelves**: a reveal is a QUESTION STANDING IN FRONT OF THE PLAYER, and no save written before this version can be holding one – the tie resolved inside the tick, reported itself in a toast and left nothing to answer. v26's `knock` case, applied to the second college fixture. ⚠ **And a week already lived is not re-offered** – v60's ruling repeated because it is the tempting mistake here: `pendingCallUp` on a migrated save may name a tie played weeks ago whose rubbers are still in the feed (`keep: true`), so a back-fill of `{week, revealed: 0}` would compile, would find its matches, and would pause a career in front of a tournament it finished before the update. The LETTERS behave the same way: `settleCallUpLetter` is guarded on the week BEFORE a tie, so a migrated career gets its next invitation and none for the ties behind it – a letter stamped at today's week about a week that already happened is a fabrication that reads like a record (`settleAcademyLetters`' own rule). ⚠ **A migrated career plays identically this week**: `callUpRevealOpen` is false, `resumeFromCollege` pauses on nothing new, `pendingView` builds nothing new. ⚠ **A SECOND FIELD AND NOT A `kind` ON v60's** – one field with a discriminator would have re-shaped a persisted object every save since v60 carries, which is a rewrite of shipped data rather than an append. Idempotent (an existing value is left alone – v30's rule), zero draws on any stream, so the frozen capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v63.json`, the recipe every fixture since v25 uses |
| v65 | world `+fieldSeasonTitles` – **WHO WON EACH AI TOURNAMENT**, by rung and then by champion id, for the season in progress. It is the SECOND HALF of v53's repair one row of this table down: v53 kept what the field EARNED, this keeps what the field WON. `runAiTournament` resolves every canonical bracket in the game and `runTournament` stamps the winner explicitly (`finishes[alive[0].id] = 0`) – and the result was then dropped three ways over: the points went to `fieldSeasonPoints` with no event and no finish attached, the ledger row was written for the LIVE cohort only (a field pro hit a bare `continue`), and the news line carries prose with **no player id**, on 6 of 16 rungs, and is skipped entirely on the event the kid entered. So the world knew its champions and no reader could name one – which is why "titles per distinct champion, ours against the real 1.84" (`docs/research/title-drought-reality.md` §2) had to be settled by arithmetic instead of read off a save. ⚠ **A TALLY, NOT ROWS**, for v53's measured reason: `world.results` is pruned on a 52-week window sized for 199 people AND is what `computeRanking` reads, so ~30 field rows a week would move the standings – a different change. Two numbers deep by (rung, champion) is at most one entry per event played, ~189 a season. ⚠ **The back-fill is EMPTY and it is a PRESERVATION**, exactly as v53's was: every career saved before this build was played on an engine that discarded the champion, so an empty tally is precisely what those seasons contained. ⚠ **And it cannot be reconstructed**, which ends the question rather than starting it: the bracket ran on `seed:aitour:<event.id>` against `deriveWeekField`'s inputs AS THEY STOOD THAT WEEK – results since pruned, a cohort since drifted, a points tally already added to – so a replay today deals a different draw and would invent a champion the save never had. ⚠ **It holds the CANONICAL champion of every bracket, the kid's event included** – unlike the news line, which prints only her own draw's winner there, because two champions of one tournament in one week's feed would be a lie about the story. The record is the field's, not the feed's, so its count equals the number of AI tournaments played. Cleared at the season wrap on the same line as `fieldSeasonPoints`. Idempotent (a non-object value is rewritten whole – v30's rule), zero draws on any stream, so the frozen capture (41550 / e6b0c709) is untouched by construction. This fixture carries a repeat champion and a second rung so the shape is frozen WITH content, the way v53's carries three scoring pros. ⚠ **This row shipped as v64 on its own branch and was renumbered on the merge of 28.08** – the wave was cut from round 28's ledger while that branch still read 63, so it took the next free number it could see, and `main` had meanwhile taken 64 for the row above. Two different v64 schemas, each correct against its own chain, and a save from either unreadable by the other. The migration moved with the number: it runs at `v === 64`, AFTER the reveal, and `tests/migrations.test.ts` walks a v63 save 63 → 64 → 65 rather than checking either step alone |

| v66 | `WorldEventCategory` `+'business'` – THE PARENT'S BUSINESSES' INCOME LINES (round 29 part four P7: «нам нужен мерч …» and «нам нужна академия, которая зарабатывает»). **A union widened and no field moved**, so the migration writes nothing – the version moves anyway by the v44 precedent verbatim («a new member of that union is a schema change by the rule in CLAUDE.md §3»): `events[].category` and `financeWeeks[].byCategory` are persisted, and a v66 save read by a v65 build would carry a category its union does not know. ⚠ **Nothing is back-filled, by construction and not by omission**: the businesses did not exist, so no save can hold a merch brand it could not buy or an academy income it was never paid – the ledger stays truthful about what it actually banked (v44's own rule), and the income starts from the career's next tick, forward-only. The two zero-cost reuses were weighed in the round-29 ledger and refused: `'income'` folds a built business into «the parents' job», and `'academy'` already means the scholarship SHE receives – two facts under one name is the defect v44 was cut to end. This fixture is v65's payload through the (empty) migration, **plus the representative content the version exists for** – an owned `merch-brand` on the shelf, one `'business'` income row in the feed and the same week's entry in `financeWeeks.byCategory` – so the shape is frozen WITH content, v65's own approach. Zero draws on any stream; the frozen capture (41550 / e6b0c709) is untouched by construction |

| v67 | owned assets `+units` (replacing the v63-v66 optional pair `basisCents`/`basisWeek`) and `+name` on the first row of a nameable family – ROUND 30 #14 («надо логику фонда переделать на покупку ДОЛЕЙ в фонде… Стоимость активов будет рассчитываться исходя из стоимости долей») and ROUND 30 #8/#10 («Merch brand давай предложим пользователю несколько вариантов именования при покупке», the academy «по принципу бренда»). ⚠⚠ **IT IS A RENUMBER, AND THAT IS THE ONLY INTERESTING THING ABOUT IT.** Both back-fills were written into the v65 -> v66 step while `main` still declared 65, which was correct then: the append-only rule bites at the moment of SHIPPING, and a version main has never seen has no saves in the world to break. Then PR #114 merged round 29 and main declared 66. Nothing about the code changed – its PREMISE expired – and the consequence was silent and total: a career already in play says `schemaVersion: 66`, so `migrateSave` skips the step, so a fund keeps `basisCents`, never gains `units`, and is then valued by a function that multiplies units by a price. **A migration that never runs is worse than one that runs wrong.** The two back-fills moved here intact and v66's step is byte-identical to main's. ⚠ The sailing-yacht rename did NOT move – it shipped INSIDE v66 (eaf61759) and is frozen there. ⚠ **What v67 reads is unchanged by the move**: `basisCents`/`basisWeek` shipped as OPTIONAL keys at v63-v65 without a version move and v66 does not touch them, so reading a v66 row is reading a v65 row. ⭐ The conversion is `units = basis / price(basisWeek)` – the row's OWN week, not today's – which preserves the entry price and the gain instead of resetting them; the name is `nameSuggestionsFor`'s first option, so a back-filled name is one the game itself would have offered. This fixture is the v66 fixture's own career with a topped-up fund, an untopped deposit, a two-stage academy and a car, carried through the step: units on the two investments, ONE name on the academy (its first row), the car untouched. Pure state, zero draws – the frozen MAIN capture cannot move. `scripts/schema-ladder.mjs` is the machine check that would have caught this, and round 30 item 25 is the lesson |
| v68 | world `+ageCurve` – `{plateauStart, declineStart, injuryFrom}`, THE TWO AGES THAT ARE HERS (ROUND 31 #10 + #13, `docs/specs/age-curve-fork-and-spread.md`). `ECONOMY.development.ageCurve` peaked every career 23-28, which the round measured against the owner's own WTA reference as **exactly the college window worn by everybody** – two to four years late for a girl who went straight to the tour. Three things move it now: the fork's ROUTE (direct 22/27, college 23/29 unchanged), a per-career SPREAD of ±1.5 years drawn once off `seed:decline`, and the weeks her body has spent off court. ⚠⚠ **The back-fill is a PIN and that is the whole point of the version move.** The owner is playing a career – Alice, week 933, 31.7, at 93.1% of her peak, a decline he has been reading for a season – and a curve re-derived from her seed on the next load would move HER clock mid-game. So every existing save is written `{plateauStart: 23, declineStart: 29}`: today's behaviour exactly, and nothing ever re-derives a stored curve. ⚠ **23 and 29 are LITERALS in the step, not `ECONOMY.development.ageCurve`** – the pin's promise is «the pair the engine read for this save yesterday», which is a historical fact and not a live constant; reading the object would hand every legacy career whatever the curve is re-tuned to next, which is the retroactive move the step exists to prevent. ⚠ `injuryFrom` is the weeks the save has ALREADY lost (`max(injuryHistory sum, careerTotals.weeksLostToInjury)` – v40's reading, reproduced rather than imported), because the pull is applied on read against that mark: a migrated career reads 29.000 on its first load however broken it is, and only the weeks it loses AFTER the update pull it earlier. A career that resolves its own curve at the fork writes 0 and pays for all of them. ⚠ The pin is UNCONDITIONAL, including for a career too young to have answered the fork – «every existing save behaves as it did» is a guarantee a player can rely on and «every existing save except the ones we judged young enough» is not. ⚠ The field is OPTIONAL because `createWorld` does not write it: the route is the fork's own answer and does not exist at week 0, and the eighteen frozen career hashes walk 156 weeks to age 16.6, so a key that is never written there cannot move one – measured, one key of sixty-one (`schemaVersion`) on `tools/frozen-key-diff.ts`. Idempotent, zero draws on any stream – the frozen capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v67.json`, the recipe every fixture since v25 uses; that career has lost 5 weeks to injury, so it carries `injuryFrom: 5` |
| v69 | world `+brandStrengthSeed` – `{week, value}`, WHERE THE BRAND'S SLOW STOCK STOOD THE WEEK IT ARRIVED (ROUND 32 #4, `docs/specs/brand-inertia-2026-08.md`). The worth of the brand stopped being this week's fame and became «the best she has ever been, faded on a half-life measured in years, never below a share of that best» – a quantity that is DERIVED from records the career already keeps. What cannot be derived is «what was this career reading the day before the update», so the step pins it: `{week: the save's own week, value: fameAt(save, that week)}`, and the derivation treats every week at or before it as answered by the pin. ⚠ The field is OPTIONAL because `createWorld` does not write it and no phase of the tick writes it either – a career started after v69 carries no pin and reads its whole own history. Idempotent, zero draws. ⚠ THIS ROW WAS MISSING and was written in the round-35 #14 wave from `migrations.ts` and `state.ts`; the fixture itself shipped with round 32 and is the real migration's output on `v68.json` |
| v70 | world `+drawnFirstRounds` – `Record<eventId, opponentId>`, WHO SHE DREW IN ROUND ONE, WRITTEN DOWN THE WEEK THE DRAW WAS SHOWN (ROUND 35 #14). The owner, 03.09: «на неделе перед турниром случилась жеребьевка, мне сказали "играем против №118 шанс 71%", пошел турнир - соперник в первом раунде №76». The draw was stored NOWHERE: `previewEvent` rebuilt the field on every read and `firstRoundOpponent` indexed into it, and although the RNG (`seed:kidtour:<eventId>`) was stable, the selection table, the rivals' conditions and her own standing all move every week – so the card at week − 1 and the bracket at week E named different girls on **293 of 489 draw weeks (59.9%)**, measured with `tools/r35-draw-fact.ts`. ⚠⚠ **The back-fill is deliberately EMPTY, which is the opposite of v68's and v69's pins and is the whole decision in the step.** Those two stored a number that was still derivable and only needed freezing; this one is not derivable any more – the world state it was a reading of has already moved – so back-filling would mean re-deriving the very quantity the item exists to stop re-deriving and calling the guess a fact. An existing save carries no draw, its first tick records one, and it costs at most the ONE tournament whose card is on screen at the moment of the update. ⚠ The field is OPTIONAL: absent means «no draw was ever shown for this event», which is true of every pre-v70 save and of an event whose week arrives before a card could exist; an unrecorded event draws live exactly as it always did. Idempotent, zero draws on any stream – the frozen capture (41550 / e6b0c709) is untouched. ⚠ This fixture is the real migration's own output on `v69.json` with ONE row added by hand (`3-w204-j30` -> `ai-0`, an event id and a player id the fixture itself already holds), because an empty object would exercise nothing about the shape |
| v72 | world `+spirit` `+bond` `+temperament` – THE PRIVATE LIFE'S TWO NUMBERS, AND WHO SHE IS (wave 1, `docs/specs/who-she-is-2026-09.md` §4 and `docs/plans/the-private-life-build.md` §§1b-1d). `spirit` is the WEATHER (0..100 in tenths, start 70 – written only by `accrueSpirit`, read only by `spiritMatchFactor` at the match seam); `bond` is the STANDING (0..100 in steps of 0.5, start 70 – moved only by parent decisions, then regressing toward 70 at 0.5/week); `temperament` is one of `sunny`/`fiery`/`quiet`/`deep`. Neither number is ever shown as a number on any surface. ⚠⚠ **The back-fill is two different kinds of thing and that is the whole point of the step.** The two numbers are the literal 70 – uniform, ruling V4 – because nothing in an older save could produce a truer figure and re-deriving «how she felt in season three» from a results ledger would be a guess wearing a fact's clothes; 70 is also ABOVE spirit's knee (60), so a migrated career reads factor 1.0 and plays byte-identical tennis until something actually moves her. `temperament` is **DERIVED, not defaulted and not drawn**: the step calls `temperamentFor` – THE SAME exported function `createWorld` calls – on the career's own seed, so an existing career does not GET a temperament, it turns out to have ALWAYS HAD one. A migration with its own copy of the two axis picks would hand a live save a different girl from the one the engine would have drawn, and nothing downstream could tell which was real. Idempotent (`??=` on three keys); MAIN untouched – the only stream reached is the purpose-scoped `seed:temperament` sub-stream – so the frozen capture (41550 / e6b0c709) cannot move. ⚠ This fixture is the real migration's own output on `v71.json`, unedited |
| v73 | world `+lifeLog` – EVERY LIFE BEAT THIS CAREER HAS LIVED (the private life, wave 2; `docs/plans/wave-2-the-reaction-surface-runbook-2026-09.md` §4). One append-only row per beat – `{week, kind, detail, answer}` – and **the row is also the queue**: `answer: null` is the pending state, so several beats in one week are answered one dialog at a time in `lifeLog` order and there is no second boolean to desync. Wave 2's wire shipped the field OPTIONAL so both halves of the wave could build against the seam before anything wrote a row; this version makes it required. ⚠ **The back-fill is `[]`, and it is EXACTLY TRUE rather than a bargain** – which puts it in a different class from every other empty back-fill in this file. v29 and v31 wrote nothing because the evidence had been PRUNED and a confident wrong answer is worse than a partial one; v26 and v32 wrote nothing because fabricating rows would mean fabricating DECISIONS the player never made. Here the reason is simpler: **a career that predates the layer has lived no beats, because there were none to live.** Zero is not a placeholder for the true value, it IS the true value – v30's `seasonRecord.wta` is the nearest relative and makes the same claim for the same reason. ⚠ **A migrated career standing on an OPEN fork is not retro-fitted with an opinion**: the `'fork-opinion'` row is raised by the tick that opens the fork and that tick has already happened for such a career, so it answers with no row on record, `answerFork` refuses nothing and the congruence delta is not charged – nobody asked her, so nobody can have overruled her. Every fork raised from this version on carries her opinion in front of it. Idempotent (one `??=` on a key nothing else touches) and it reaches no sub-stream at all, so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v72.json`, the recipe every fixture since v25 uses |
| v74 | world `+loveEpisodes` – EVERY ATTACHMENT THIS CAREER HAS LIVED (the private life, wave 3; `docs/plans/life-wave-3-builder-2026-09.md` §2 T1, design `docs/plans/the-private-life-build.md` §4 step 3). One append-only row per attachment, never pruned – `{id, sinceWeek, endedWeek, knownWeek, wants, partnerId}` – and **the ACTIVE attachment is DERIVED rather than stored**: the last row with `endedWeek === null`, read through `activeEpisode` (engine/world/lifeBeat.ts) and through nothing else. ⚠⚠ **Episodes rather than a nullable slot, and that is the whole decision in the step** (the 09.09 re-cut, review find #5): a romance that begins AND ENDS before the parent ever knew must survive save and reload intact and surface later as one honest late row, which a stored «current partner» would have overwritten out of existence the next time someone appeared. ⚠ **No name and no gender is persisted**, deliberately – the schema must not hardwire boyfriend->husband; who the partner is arrives with step 6's fictional-name pass and the owner's word. ⚠ **The back-fill is `[]`, and it is EXACTLY TRUE rather than a bargain**, in v73's own sense one rung down: v29 and v31 wrote nothing because the evidence had been PRUNED and a confident wrong answer is worse than a partial one, v26 and v32 because fabricating rows would mean fabricating DECISIONS the player never made – here a career that predates the layer has lived no attachments, because there were none to live. Zero is not a placeholder for the true value, it IS the true value. ⚠ **And an empty list stays a first-class outcome for ever**: «no romance at all» and «never latches» are real results of wave 3's arrival hazard, not «not yet computed», so nothing downstream may read `[]` as a defect to repair. ⚠ A migrated career is not retro-fitted with a past either – the hazard runs on the tick (`seed:life:arrival:<week>`) and every tick such a career has already taken happened before the hazard existed, so she arrives with nobody and the first person who can appear appears on her next eligible week. Idempotent (one `??=` on a key nothing else touches) and it reaches no sub-stream at all, so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v73.json`, the recipe every fixture since v25 uses |
| v75 | world `+spiritShock` – WHAT AN ENDING LEFT ON HER (the private life, wave 4; `docs/plans/life-wave-4-builder-2026-09.md` §2 T1/T3, design `docs/plans/the-private-life-build.md` §5 step 4). `{week, kind}` while an ending is still sitting on her, `null` otherwise – non-null from the week an attachment ends until spirit has climbed back to within two points of her own baseline, where `accrueSpirit`'s tail clears it. ⚠⚠ **It is a MARK and never the physics, and that is the whole decision in the step.** The drop (−22 steady / −34 intense) goes through the standing perturbation path and the recovery is the standing weekly return with no special curve at all; what this field adds is the one fact the numbers cannot state – that a live shock is WHY she is under her line, since a spirit of 48 looks identical whichever way it got there. ⚠ **`kind` is a union with one member today, on purpose**: `'breakup'` is wave 4's and the build plan's steps 7–8 add the others, where a bare boolean would have to be widened by a migration and a union widens in place. ⚠ **The back-fill is `null`, and it is EXACTLY TRUE rather than a bargain**, in v73's and v74's own sense one rung further on: v29 and v31 wrote nothing because the evidence had been PRUNED and a confident wrong answer is worse than a partial one, v26 and v32 because fabricating rows would mean fabricating DECISIONS the player never made – here a career that predates the layer carries no live shock, because nothing in its past could have shocked her. Null is not a placeholder for the true value, it IS the true value. ⚠ **The same bump carries `WorldEvent.lifeKind?` and that field is owed NO back-fill**, said here so a reader does not think it was forgotten: it is optional and additive on a feed ROW rather than on the world, absent means exactly what every historical row already means («this row carries no life-kind discriminator»), and nothing writes it before T5 – so there is no shape for a migration to repair, and stamping `'met'` onto the `'life'` rows an old save happens to hold would be re-deriving a fact from prose and calling the guess a record (v70's own refusal). The feed's glyph column keys on `lifeKind ?? 'met'`, so a wave-3 row keeps its 🤍 untouched. Idempotent (one `??=` on a key nothing else touches – and `null` is also the value `??=` tests for, which costs the step nothing since «absent» and «already null» mean the same thing) and it reaches no sub-stream at all, so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v74.json`, the recipe every fixture since v25 uses |
| v76 | world `+psychologistHired` `+psychologistRung` `+psychologistFocus` `+psychologistFocusSeason` `+wallsLean` `+wallsFlipped` – THE PSYCHOLOGIST'S YEAR, AND HER WALLS (wave 5; `docs/plans/life-wave-5-builder-2026-09.md` §2 T1, the walls model verbatim from `docs/specs/who-she-is-2026-09.md` §2a). **Six keys in one append, and they are two different kinds of thing riding one bump** – four are the STAFF SEAT (hired, at which rung, working on which focus, set in which season), shaped on v59's masseur block; two are the §2a LEANINGS and their hysteresis state, a fact about her that no player ever chooses. They land together because the wave that reads them is one wave and a schema move costs a fixture, a peel rung and ten e2e regenerations whether it carries one key or six (v72's three-in-one-append is the precedent). ⚠⚠ **Every back-fill is EXACTLY TRUE rather than a bargain**, in v73's / v74's / v75's own sense one rung further on: `psychologistHired` is `false` because the seat did not exist, so nobody was ever hired into it; `psychologistRung` is `1`, the MIDDLE rung and meaningless until hired – a dial has to read something and the shipped default is the only answer that invents no decision, which is v59 back-filling `4` sessions onto careers that had never met a masseur, verbatim; `psychologistFocus` and `psychologistFocusSeason` are `null` because nobody was ever asked what to work on, so no season holds a pick; `wallsLean` is `{open: 0, reg: 0}` because **zero is the identity and not a placeholder for one** – a leaning of 0 means «expression equals nature», which is what a career that predates the walls has always been; `wallsFlipped` is `{open: false, reg: false}` because nothing has flipped, since nothing could have. ⚠⚠ **And the zero leaning is what makes a migrated career play byte-identical tennis**: `expressedTemperamentOf` (engine/spirit.ts) returns BIRTH while nothing is flipped, so every mechanic wave 5's T7 re-points reads exactly the value it reads today – the migration is not «the girl acquires walls», it is «the girl has always been exactly herself», written down for the first time (v72's `temperament` note in a second costume). ⚠ **`temperament` itself is untouched and always will be** – identity is IMMUTABLE (§2a's 09.09 re-cut); what drifts is walls and regulation, and the voice bibles go on reading birth alone (§3's fence). ⚠ **The rung literal is deliberately not `ECONOMY.psychologist.defaultRung`**: a shipped migration must never change what it back-fills because somebody later retuned a constant, which is why v59's step writes `4` while `createWorld` reads the constant. Idempotent (six `??=` on keys nothing else touches – `??=` and never `||=`, because three of the six are `false`/`0`-shaped and would be replaced on every load) and it reaches no sub-stream at all, so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. T1 ships the seats and NO writer for any of the six – `hirePsychologist` is T2, the focus command T3, the weekly leaning pass and the flip hazard T7 – so the version is inert by construction and the frozen careers prove it. This fixture is the real migration's own output on `v75.json`, the recipe every fixture since v25 uses |
| v77 | world `+spotlightHabituation`, AND FOUR FIELDS ON EVERY `LoveEpisode` ROW – `+publicWeek` `+publicWrong` `+airedMetWeek` `+airedEndedWeek` – THE SPOTLIGHT (wave 6; `docs/plans/life-wave-6-builder-2026-09.md` §2 T1, the model `docs/specs/who-she-is-2026-09.md` §3c and §3c-bis, the booth's boundary `docs/plans/the-way-she-sounds-2026-09.md` C4). ⚠⚠ **The first step in this whole ladder that back-fills fields on the ROWS of a list rather than keys on the world**, and that is the thing to carry away from it: v73, v74, v75 and v76 are all `??=` on a key of `save`, and four of this version's five fields live inside `save.loveEpisodes`, which no top-level assignment can reach. So the step WALKS the list and repairs every row – a career with two attachments needs both. ⚠⚠ **Every back-fill is EXACTLY TRUE rather than a bargain**, in v73's / v74's / v75's / v76's own sense one rung further on: `spotlightHabituation` is `0` because **zero is the identity and not a placeholder for one** – it counts «known weeks» actually lived toward `habituationFullWeeks` and a career that predates the spotlight has lived none of them, since nothing was counting and there was no pressure to acclimate to; `publicWeek` is `null` because the world never learned, and that is null rather than «week 0» – the press did not exist as a mechanic; `publicWrong` is `false` because no story ran, so no story ran wrong (the flag is meaningless while `publicWeek` is null); `airedMetWeek` and `airedEndedWeek` are `null` because the booth has voiced neither fact, there being no channel for it to voice one through. Not one of the five reconstructs anything: the version is «nothing about her private life was ever public, and she has never lived a week known», written down for the first time. ⚠⚠ **And that is what makes a migrated career play byte-identical tennis**: every wave-6 mechanic is gated on the news standing (`newsStandingOf`, D1's rank bands) or on a non-null `publicWeek` (T6/T7), and at these five values not one of them can fire. ⚠ **`publicWeek` lives beside `knownWeek` and the two never merge** – `knownWeek` is when the PARENT found out, `publicWeek` when the PRESS did, and either can come first: the founding scene of §3c-bis is a parent reading a headline about a daughter who never told him. Idempotent (`??=` and never `||=` – `publicWrong` is `false`-shaped and a `publicWeek` of `0` is a real week, both of which `||=` would clobber) and it reaches no sub-stream at all, so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. T1 ships the five seats and NO READER for any of them – `world/spotlight.ts` and `newsStandingOf` are T2, the pressure T3, habituation T4, the fifth focus T5, the leak T6, the booth stamps T7 – so the version is inert by construction and the frozen careers prove it. ⚠ **This fixture is the real migration's own output on `v76.json` and it carries `loveEpisodes: []`, like every golden before it** – said here rather than left as a gap. Measured 14.09: not one of the 78 golden saves has ever held an attachment, so the corpus cannot witness the per-row walk at all, and a fixture regenerating green would be the receipt for work nothing did. The witness is CRAFTED instead – `tests/wave6-spotlight-schema.test.ts` §B builds a v76 payload carrying two rows, one live and one ended, and asserts all four fields arrive on both – and the frozen corpus adds one real row through `FROZEN.eliteGrinder` |

| v78 | world `+composureBonus` `+sparringHired` `+sparringRung`, AND `+entries` ON EVERY `OwnedAsset` ROW – **ONE BUMP, THREE CUSTOMERS**, which is the owner's own scheduling (15.09: «41 #22 давай тоже в v78 закинем») and the whole economy of a schema move: the ritual costs a fixture, a peel rung and ten e2e regenerations whether it carries one key or four. ⭐⭐⭐ **`composureBonus` is the only quantity in this game that lives ABOVE a rolled ceiling** (round 42 #35). It is HEADROOM and not points – `composureCeilingOf` adds it to `potential.composure`, and ordinary development does the climbing – so a bonus point is earned twice, the seat buys the room and the training fills it, and nothing anywhere adds to `skills.composure` behind `growWeek`'s back. Earned at 1/52 of a point a week while the psychologist works the `'coolhead'` focus, lost at 0.2/52 while he does not, bounded at 0 and at +5: the three season figures are the owner's verbatim («+5 потолок, по очку за сезон… 0.2пп за сезон без этой тренировки») and they live in `ECONOMY.psychologist`. ⚠ **The decay is scoped to the bonus alone** – his own clarification, «чтобы у нас обычный естественный прирост тоже работал… в зависимости от сида» – so below her rolled ceiling composure grows exactly as it always has and `composureEase` is bounded by `min(this week's fall, how far she is above the NEW effective ceiling)`, which is what keeps it off training's gains and off `veteranPoise`'s excess. ⚠ **The architect's proposal named an «age-creep clamp that already exists» and there is none** – composure's `loss` is 0 by `isPhysicalSkill`, `gain`/`veteranPoise`/`coolhead` are all ≥ 0 and `Math.max(d.floor, …)` is a FLOOR, so composure cannot fall anywhere in `development.ts`. The easing is therefore BUILT, narrow, and named; measured rather than inherited. ⭐⭐ **`OwnedAsset.entries` is round 41 #22's month-old wait**: `{week, cents, units?}` per purchase, appended by `buyAsset` and rewritten by nothing, which is what lets the fund chart mark WHEN the family bought and WHAT they paid that week. It is REQUIRED, and that is why v78 is a schema move rather than an optional key – four fields on that same interface shipped with no bump because absence already meant something true, and here absence would mean «never recorded», which is true of every historical row and false of every new one. The back-fill is `[]` and not one reconstructed entry: `paidCents` is a blended net figure a top-up adds to and a part sale scales down, so the degraded single mark would print a sum the family never paid in any single week – round 41 measured that and refused it. ⭐ **And unlike v77's per-row step, the corpus CAN witness this one**: fourteen fixtures carry real asset rows – v65 (2), v66 (1), v67–v78 (6 each), 75 rows in all – so the loop executes on the corpus instead of zero times. ⚠ And «reaches it» is not «checks it»: the corpus asserted nothing about `assets` until this version added the per-fixture walk, so the skipped-row mutation measured 3 RED with all 78 fixtures green beside it, and 16 RED afterwards. `migrations.ts`'s standing note re-measured rather than inherited, and sharpened. ⚠⚠ **`sparringHired` / `sparringRung` are the keys ONLY and nothing on this tree reads them** – the seat's behaviour is round 42's bundle 13. Two fields cannot justify a bump of their own, so they ride the one two other customers were already paying for; a reader who finds an unused key here is reading a SCHEDULING decision, not a half-built feature. The back-fills are `0` (⭐ zero is the identity: a career that predates the mechanic has been carried nowhere above its ceiling, and at 0 the effective ceiling IS `potential.composure` to the bit, so `growWeek` is byte-identical), `false` (the seat did not exist), `1` (the default rung, meaningless until hired – v59's masseur dial and v76's psychologist rung quoted rather than re-argued) and `[]`. Zero draws on any stream – three `??=` on world keys, one per asset row, all literals – so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. This fixture is the real migration's own output on `v77.json`, the recipe every fixture since v25 uses |
| v79 | world `+coachPairs` `+sparringTravels` – **ONE BUMP, TWO CUSTOMERS** (the chemistry wave C1, `docs/specs/the-chemistry-2026-09.md`; and round 42 #48's third sparring key, which v78 was scoped before the owner gave it). ⭐⭐⭐ **`coachPairs` is the first thing in this game that remembers a RELATIONSHIP.** `Record<coachId, { chem, phase, standing }}` – how she and that man get on (-100 .. +100), the weather the level is accruing under (-1 .. +1), and a score wave C2 will read. The owner, 16.09: «эта самая химия может как-то нарабатываться с разной динамикой – это может стать показателем, насколько ей комфортно с тренером». ⚠ **It has to persist because it is PATH-DEPENDENT**, and the spec's own first draft got this wrong: it said the rate «is not persisted at all: it is a pure function of (seed, coachId)», and that stopped being true the moment the rate started moving with results and with her state. ⭐ **What IS still pure is the AFFINITY** – `affinityFor` is drawn from `(seed, coachId)` and nothing else, re-derived at every call site and stored nowhere, and that is the half which keeps «every variation reproducible» true rather than aspirational. The roster's drawn `manner` and `style` are not persisted either, for the same reason. ⚠ **ONE KEY WITH THREE NUMBERS AND NOT THREE PARALLEL MAPS** (spec §10): all three are facts about one PAIR, written on the same week by the same pass, and three maps keyed on the same coach id would be three chances for them to disagree about who exists. `standing` is written and never read on this tree – wave C2's, and v78's sparring pair is the precedent for riding one bump a wave early. ⚠ **The back-fill is `{}` and it is exactly true rather than a placeholder**: the map is SPARSE by design, a row appears on the first week she trains with that man and never before, so an empty map is the complete statement that a career predating the mechanic accrued nothing with anybody – there was nothing to accrue. The tempting reconstruction (a row for `coachId`, back-dated from the weeks he has been hired) was refused for the reason the spec argues itself: the level depends on results and on her state, neither of which the weeks-hired count knows, so it would have handed a loaded career a history it never lived. ⚠⚠ **AND THIS IS THE FIRST VERSION SINCE v78 WHOSE MECHANIC IS REACHABLE IN A FROZEN CAREER.** v75/v76/v77 shipped seats with no reader; v78 shipped one no frozen career could reach. Every frozen career hires a coach, so every one of them now has a relationship – the eighteen constants move for a REASON rather than by a key append, and the protocol in `tests/coachTravelEdgeFixtures.ts` governs the re-stamp. `sparringTravels = false` is `sparringHired`'s own v78 answer quoted rather than re-argued: the seat did not exist, so nobody was in it and nobody was travelling; nothing on this tree reads any of the three sparring keys and the seat lands with wave F1. This fixture is the real migration's own output on `v78.json`, the recipe every fixture since v25 uses. |
| v80 | world `+form` – **WAVE F1, HER FORM** (`docs/specs/the-form-and-the-sparring-2026-09.md` §1 and §6). **ONE key, ONE customer, and the narrowest step this ladder has taken since v70.** Tenths, 0-centred, clamped `[-10, +10]`. ⭐ **The back-fill is `0` = neutral and it is the IDENTITY rather than a placeholder for one** (v78's `composureBonus` rule, quoted because the argument transfers exactly): 0 is not «we do not know how she is playing» – the number is 0-CENTRED, so 0 IS «she is exactly the player her results say she is», which is the true and complete statement about a career that predates the mechanic. At 0 `formComposureDelta` returns an exact 0 and `kidMatchPlayerFor` takes its untouched early return, so a resumed save plays byte-identically until a match or a gap moves her. ⚠⚠ **The tempting reconstruction was refused for v79's own reason, one version on**: form could have been back-dated off the results ledger the save still holds, and it could not honestly – `results` prunes at 52 weeks, the residual needs the PRE-MATCH probability and therefore both players' snapshots, and the rhythm channel needs a gap history the pruned ledger cannot supply. A reconstruction would invent a slump the career never lived through; 0 costs the resumed career the form it never had, which is the honest half of the trade and the only one that is true. ⚠⚠ **AND IT IS NOT INERT, WHICH IS HALF THE MOVE.** v75–v78 each shipped seats with no reader and v78 shipped one no frozen career could reach; this one ships WITH its reader – the weekly pass in `world/phaseHerWeek.ts`, and `composureEff` at `MatchPlayer` build time – because a slump nobody can feel is the decorative mechanic round 42 found twice. **So the frozen careers move, and they move for a REASON rather than by a key append**: every frozen career plays matches and has matchless weeks, so every one of them now carries form into its own results. Diffed per key before it was believed (`tools/frozen-key-diff.ts`, control = this wave's own change neutralised IN PLACE), and re-stamped with the dated note the protocol asks for. ⭐⭐ **`PRE_V80` is the first rung in the file where ALL THREE cells moved**, and the contrast with the rung directly above it is the reason worth carrying: v79's chemistry could not reach `selfTravelling` at all – no coach, no relationship – so `PRE_V79` carries the VERBATIM v78 value in that cell and says so in a note. Form reaches her, because she plays matches and sits out weeks whether or not anybody is in her corner. ⚠ **The seat's three keys finally gain their reader in the same wave, with NO key of their own**: `sparringHired` / `sparringRung` (v78) and `sparringTravels` (v79) are read by `world/sparring.ts` from this version on – which is what those two versions' «a reader who finds an unused key here is reading a SCHEDULING decision» was promising. ⚠ **`??=` and never `\|\|=`**, v76's / v77's / v78's / v79's rule for the identical reason, and **this key is the sharpest case the rule has had**: `form \|\|= 0` is not merely a no-op that looks like a write, it would silently overwrite a live 0 on a re-run – and 0 is the value MOST careers hold. Idempotent and draw-free (one `??=` on one world key, gated on `v === 79`, writing a literal); `seed:form:<week>` stays RESERVED and unused (O4), so no sub-stream is reached and the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. ⭐ **Verified rather than asserted**: `migrateSave(v79.json)` is byte-identical to this fixture, its only new key is `form`, and the only shared key whose value moves is `schemaVersion` itself. This fixture is the real migration's own output on `v79.json`, the recipe every fixture since v25 uses. |
| v81 | **NOTHING ON THE WORLD, AND ONE OPTIONAL FIELD ON A `lifeLog` ROW** – `LifeBeatRecord.frame?`, the id of the delivery scene a `'small-talk'` beat was raised in (round 44; `docs/specs/the-frame-pool-2026-09.md`, the third of its three mechanical rulings). ⚠⚠ **This is the first step in the ladder whose BODY IS EMPTY, and the empty body is the design rather than an oversight.** The key is optional, it lives on a row, and its ABSENCE is a true statement about every historical row: nothing drew a frame before this version, so there is no value a back-fill could write that would be true. The read side handles it – `smallTalkFrameOf` renders a frameless row with the FIRST line of its presence's pool, and `kettle` / `call-middle` are exactly the two frames the shipped catalogue wrapped `practice-clicked` in, so a small-talk row already in a save reads back byte-identically to what it showed on the week it was raised. ⚠ **The tempting back-fill was refused for a reason worth keeping**: a loop writing `row.frame ??= 'kettle'` would stamp a kitchen onto rows raised from a dormitory – the read side knows the presence, a migration does not. ⚠⚠ **So why a VERSION at all, when `LifeBeatRecord.heard` took none?** Because of the owner's own third clause: a frame may not change after a save, a reload, **or the pool growing**. A frame derived from a purpose-scoped stream keyed on the career week survives the first two perfectly and cannot survive the third – a pool of ten re-derives a different member for a beat already on screen – so the chosen id is read back rather than re-derived, and a field that is read back is state. ⚠ Zero draws on any stream in the step (it writes nothing at all), so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction; the frame itself is drawn on `seed:smalltalk:frame:<week>`, a purpose-scoped sub-stream. ⚠ **The round's big half – the corpus, 8 situations to 51 – does not reach the frozen careers**, because 156 weeks never leaves `school` and no corpus row declares it; the eleven live-shaped cells moved on the new key alone and all 84 `PRE_V*` rungs held. This fixture is the real migration's own output on `v80.json`, the recipe every fixture since v25 uses. |
| v82 | world `+coachDeal` – **THE AGREED WEEKLY FIGURE, WRITTEN DOWN** (round 42 #51, ruled 17.09 in round 44; `docs/specs/the-coachs-raise-2026-09.md`). **ONE key, ONE customer.** The owner, having watched his coach's weekly price fall 2.2k -> 1.8k across a season that went well: «мне кажется это не корректно»; and on the fix: «"зафиксировать при найме и пусть просит, как массажист" – верно». Until this version the hired man's figure was stored NOWHERE – it was re-derived every week as `facilityRateCents(her age, tier) + round(max(0, rate − court) × coachRetainerBand(her WTA rank))` – so the family's payroll moved with her results and her birthdays, and it could FALL. `coachDeal` holds the contract: his agreed LABOUR rate above the court, the week it was struck, the four marks the next annual ask is judged against, and the residual banked since. ⭐ **The back-fill is `null` and it is a LITERAL, which is this file's own standing rule kept rather than argued around**: a shipped migration must never write a value derived from `ECONOMY`, and `null` is in any case TRUE of every save ever written – nobody had agreed a figure in writing, because until this version there was nowhere to write one. ⚠ **The literal costs the family nothing because the first tick settles it**: `settleCoachDeal` runs inside `resolveBaseCosts` on every week, meets a hired coach with no contract on the first week after the upgrade, and writes one at the market's own quote – the exact cents that same till was about to charge, so the bill does not move on the upgrade week. It dates the deal from `coachSinceWeek`, the ledger's own tagged record of when the arrangement began, so a family three years into a partnership keeps its anniversary rather than starting a new clock; the MARKS are taken at the settle week, because today is all a migrating save can honestly know. ⚠ **The billed rate can never exceed the one that floated** – it is `court + min(agreed, market labour)` – so this change can lower a family's payroll and cannot raise it. ⚠ Zero draws on any stream: the step writes one literal, and the ask it enables is a weighted mean over state the tick has already written, so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. ⭐ **Verified rather than asserted**: `migrateSave(v81.json)` is byte-identical to this fixture, its only new key is `coachDeal` (last in the serialisation, which is what the peel rung depends on), and the only shared key whose value moves is `schemaVersion` itself. This fixture is the real migration's own output on `v81.json`, the recipe every fixture since v25 uses. |
| v83 | **NOTHING ON THE WORLD, AND TWO FIELDS ON EVERY `LoveEpisode` ROW** – `+latchedWeek` `+partnerName`, THE WEDDING (wave 7; `docs/plans/life-wave-7-builder-2026-09.md` §2 T1, the design `docs/plans/the-wedding-and-the-children.md` §1). ⭐⭐⭐ **`latchedWeek` is the marriage latch and it lives ON THE EPISODE ROW, never as a global boolean** – the 11.09 re-shape, on the owner's own «а свадьба может быть у нас не одна, кстати?»: a marriage is a property of one episode, a divorce (if ever built) is an ending on a latched episode, and a SECOND wedding is the same machinery re-entered on a later row, zero migrations later. Written by T3, `ECONOMY.wedding.weeksAfterEngagement` weeks after the `'engaged'` beat is answered – any answer, opposing does not stop it (SHE decided; the parent's answer moved `bond`, not the calendar). ⭐⭐ **`partnerName` is written ONCE, at the engagement beat, by the ONE derivation function** (`partnerNameFor`, uniform on `seed:life:partner-name:<episodeId>`) **and persisted rather than re-derived at read** – `temperamentFor`'s and `LifeBeatRecord.frame`'s own rule: a later pool edit must never rename a husband an old career already has. First names only, so no real surname is constructible (house law satisfied by construction). ⚠ **Both back-fills are `null` and both are EXACTLY TRUE rather than bargains**: no career has ever reached a wedding, because until this version there was no wedding to reach, and nobody was ever named, because the naming pass arrives with this version – readers fall back to the unnamed phrasing they use today. ⚠⚠ **The migration WALKS the list and `??=`s each row – v77's per-row step is the precedent, and its standing note is finally answered on the corpus side**: every fixture below v83 carries `loveEpisodes: []`, so the walk runs zero times on all of them, and THIS fixture is the FIRST golden save that HOLDS an episode row – which is what lets the corpus loop in `tests/goldenSaves.test.ts` read the two fields off a real row at last. The crafted witness (two rows, live + ended; the mutation ledger with the skip-a-row and `||=` arms) is `tests/wave7-wedding-schema.test.ts`. ⚠ **A DELIBERATE DEPARTURE FROM THE v25 RECIPE, recorded rather than done quietly**: `migrateSave(v82.json)` would have produced one more empty-list fixture and kept the corpus blind to per-row steps forever. This fixture is instead a PROBE career – played by the engine's own loop (`openCareer(PRESETS[8], 0, POLICIES[0])`, `stepCareerWeek` per week with the standing drains: knocks in the step, the neutral birthday answer, the bond-neutral life-beat drain; MAIN drawn through `resumeMain(world.rngMain)` so the persisted position is true): the grinder arm to week 137 (the first week it holds an attachment – the wave's original stop), then the PLAYER arm (`POLICIES[1]`: the reserve, the coach review) from 138, because the grinder latches bankruptcy at week 207, age 17, before the fork can ever be asked; walked to the fork school's end raises at week 242, the fork answered `'continue'` (the direct route), and the plan tab saved once – the worker's own two lines, `planFromWeek(planWeek(plan))` behind `planShapeError` – before the save was taken. **Stopped at week 242, and the longer walk is the 18.09 repair**: the full gate found six corpus invariants the week-137 probe could not satisfy, every one of them content the migrated lineage always inherited and a fresh career must EARN – so the probe earns it. It now holds TWO episode rows (one live, one ended – the per-row walk runs twice), THREE college quotes on the still-open offer (`goldenSaves-quote`'s carrier floor), `plan.week` (plan.test §2), and an `ageCurve` the fork itself resolved – `injuryFrom: 0`, the direct route's pair with the seed's own spread, which is why the round31 sweep reads this fixture by its own arm: `resolveAgeCurve` adds a continuous spread and no honest engine output lands on the migration's exact 29.0. ⚠ NO `brandStrengthSeed`, and that is the shape's truth rather than a gap – the key is written once, by the v68 -> v69 migration, «and by nothing else – not `createWorld`, not any phase of the tick» – so an honest post-v69 career cannot carry one (round32's fixture case is re-aimed at `v69.json` accordingly, and round42's convergence pin at the lineage through `v82.json`). NEVER hand-crafted: every byte is the engine's own, the e2e generator's own law – the week-137 prefix reproduced byte-identical before the walk was extended. ⚠ The `'wedding'` milestone member rides this same bump (T3's album entry; the v44 'facility' / v66 'business' precedent – a new persisted union member is a schema change), and no fixture below v83 can hold one. ⚠ Zero draws on any stream in the step (two `??=` per row, literals); the wave's own draws are `seed:life:wedding:<week>` and `seed:life:partner-name:<episodeId>`, purpose-scoped sub-streams – the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. |
| v84 | world `+prologueTrace` – **THE ALBUM's ONE SCHEMA MOVE** (`docs/specs/the-album-2026-09.md` §3, his ruling of 19.09 – path (а): «хорошо бы, чтобы в финальный альбом что-то оттуда попадало тоже вообще»). **ONE key, ONE customer.** The compact slice of the finished `PrologueRun` – `picks`, `entries`, `opens`; the origin stays on the profile – written ONCE at the handover by `createWorld` from `PrologueHandover.trace` (built by `traceOf`, src/prologue/run.ts) and by nothing else: not a migration, not any phase of the tick. Until this version the prologue threw the whole record away at the handover, and the album's first chapter had nothing to be written out of. ⭐ **The back-fill is `null` and it is EXACTLY TRUE rather than a bargain** – his own word on the missing history: «это не страшно». No save written before this version kept a childhood's record, and a wizard career never walked one; for both, «no record» is the complete statement, and the album's first chapter honestly does not exist for them. ⚠ **The tempting reconstruction was refused for v80's own reason**: the run is the PLAYER's walk through the nine cards, not a function of the seed, so there is no value a back-fill could write that would be true. ⚠⚠ **THE RECIPE IS THE v25 ONE, RESUMED – `migrateSave(v83.json)` byte for byte** (verified: the only new key is `prologueTrace`, last in the serialisation, which is what the peel rung depends on, and the only shared key whose value moves is `schemaVersion` itself). The departure question v83 answered does NOT arise here and the choice is recorded rather than made quietly: a walked probe SKIPS the prologue by construction, so no engine-played fixture can hold a non-null trace, and the corpus therefore witnesses the BACK-FILL – which is what the corpus is for – while the NON-NULL shape's witness is `tests/album-trace-schema.test.ts`, through the real writer (`createWorld` handed a real run's `traceOf`, never a hand-written world), with the `??=`→`||=` and walk-removed mutation arms beside it. ⚠ Because this fixture is v83's probe career one version up, the round31 age-curve sweep reads it by the same arm as v83.json (the fork's own resolution, not the migration's 23/29 pair). ⚠ Zero draws on any stream in the step (one `??=` on one world key, writing a literal); the album's own draws live on `seed:album:flavour:<sheet>`, a purpose-scoped sub-stream re-derived at assembly – the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. |
| v85 | world `+pregnancy +children` – **THE PREGNANCY AND THE RETURN** (wave 8 T1; `docs/plans/life-wave-8-builder-2026-09.md` §2 T1, the design `docs/plans/the-wedding-and-the-children.md` §5, steps W3+W4). **TWO keys, and a THIRD move that costs no step at all.** `pregnancy` is the one she is carrying or `null` – `{episodeId, announcedWeek, pausesWeek, dueWeek, support, returnPlan}`, hung off the world with `episodeId` pointing BACK at the latched episode, which is the direction that lets a SECOND pregnancy (W5's, confirmed wanted 11.09) re-enter the same seat with zero migrations. `children` is the born – append-only rows of `{bornWeek, sex}`, on the WORLD rather than on the pregnancy because **a birth must land somewhere the week it happens** and the pregnancy that produced it is cleared that same week: a child recorded only there is a child recorded nowhere, and W5 would have to open by re-deriving children out of episode history, which is the one thing this layer's whole discipline refuses. ⭐ **`sex` is a RULED SCAFFOLD and it is written LITERALLY `'girl'`** – his words, 20.09: «пол нужен, но мальчиков у нас пока нет, можно сделать заготовку, но пока будут только девочки» – so **no stream is drawn for a constant** (a draw whose outcome is fixed is a draw-and-discard, which invariant 2 forbids by name), and the key `seed:life:birth:<episodeId>` is RESERVED IN WRITING for the day boys exist, scoped to the EPISODE rather than the week so persisted rows keep old careers' children stable when it comes. ⚠⚠ **The third move needs no step: `spiritShock.kind` widens `'breakup'` -> `'breakup' | 'postpartum'`** (the build plan's step-7 row reserved exactly this, and the field's own note predicted it in as many words). Type-level only, with NO DATA TO MIGRATE – and that is arithmetic rather than a bargain: **no save can hold the new member because nothing has ever written it**, and WIDENING a union can never invalidate a stored value. Narrowing one is the direction that costs a migration. ⭐ **Both back-fills are EXACTLY TRUE and neither is a bargain with a pruned log – this is `loveEpisodes`'s v72 argument and emphatically NOT `prologueTrace`'s v84 one a row up.** There, a real childhood had been walked and thrown away, and the row above had to explain why reconstructing it was refused. Here nothing is even TEMPTING: no save written before this version could hold a pregnancy or a child, because there were none to hold – the mechanic arrives WITH this version – so «not expecting» and «no children» are the complete statement about every career in the corpus, and no evidence anywhere in a save could say otherwise. ⚠ **T1 SHIPS THE SEATS AND NO WRITER AT ALL**, which is what «the schema move is inert» means and what the frozen careers measure: the hazard that sets `pregnancy` is T2's, the pause T3's, the `children.push` and the `'postpartum'` shock T4's. Nothing on this tree can put another value in either key, so **a pregnancy cannot exist in a golden fixture yet** – the corpus witnesses the BACK-FILL, and the non-null shape's witness is `tests/wave8-pregnancy-schema.test.ts`, which crafts one and carries its mutation ledger. T11's e2e regeneration is where the first live one appears. ⚠⚠ **THE RECIPE IS THE v25 ONE, RESUMED – `migrateSave(v84.json)` byte for byte**, verified: the only new keys are `pregnancy` and `children`, LAST in the serialisation and in that order (which is what the peel rung depends on – `careerHashAtSchema` peels in reverse, `children` before `pregnancy`), and the only shared key whose value moves is `schemaVersion` itself. The departure question v83 answered does not arise: this fixture is v83's probe career two versions up, so it still holds TWO `loveEpisodes` rows and the corpus's «the walk must be exercised» law is satisfied by the fixture it inherits. It therefore reads by the same round31 age-curve arm as v83.json and v84.json (the fork's own resolution, not the migration's 23/29 pair). ⚠ Zero draws on any stream in the step (two `??=` on two world keys, writing two literals); the wave's own draws, when its later tasks land, live on `seed:life:pregnancy:<week>` and `seed:life:return:<week>` – purpose-scoped sub-streams that do not exist on the T1 tree at all – so the frozen MAIN capture (41550 / e6b0c709) is untouched by construction. ⚠ **The frozen careers were MEASURED, not predicted at**: the per-key diff was the builder's first command on the untouched tree, and exactly three lines moved on each of the three careers – `schemaVersion`, `pregnancy` appearing at the sha256 of `null`, `children` appearing at the sha256 of `[]` – with 94 / 94 / 95 other keys byte-identical (95 / 95 / 96 keys before the append, 97 / 97 / 98 after). `PRE_V85` holds the VERBATIM v84 constants. |

`season` / `events` arrays are trimmed to a couple of representative entries (the migration never
regenerates a v6+ world, so their contents are exercised as *shape*, not size) to keep the fixtures
readable.

When you add a new fixture, keep it small but structurally faithful to that exact schema version.
